# Supabase configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api

# =============================================================================
# Server-side environment variables
# =============================================================================

# Supabase project URL (used by Express server)
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon (public) key (used by Express server for JWT validation)
SUPABASE_ANON_KEY=your-anon-key-here

# Default owner for new projects (used when no project exists)
# Set this to the email you'll use when testing in dev mode
DEFAULT_OWNER=owner@example.com

# Server port (optional, defaults to 3001)
PORT=3001

# =============================================================================
# Client-side environment variables (Vite requires VITE_ prefix)
# =============================================================================

# Supabase project URL (used by React client)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon (public) key (used by React client for auth)
VITE_SUPABASE_ANON_KEY=your-anon-key-here
