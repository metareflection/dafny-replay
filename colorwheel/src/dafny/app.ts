// Generated by dafny2js
// Do not edit manually - regenerate from Dafny sources

import BigNumber from 'bignumber.js';

// Configure BigNumber as Dafny expects
BigNumber.config({ MODULO_MODE: BigNumber.EUCLID });

// Import the generated code as raw text
import dafnyCode from './ColorWheel.cjs?raw';

// Set up the environment and evaluate the Dafny code
const require = (mod: string) => {
  if (mod === 'bignumber.js') return BigNumber;
  throw new Error(`Unknown module: ${mod}`);
};

// Create a function that evaluates the code with proper scope
const initDafny = new Function('require', `
  ${dafnyCode}
  return { _dafny, ColorWheelSpec, ColorWheelKernel, AppCore };
`);

const { _dafny, ColorWheelSpec, ColorWheelKernel, AppCore } = initDafny(require);

// ============================================================================
// Helpers
// ============================================================================

// deno-lint-ignore no-explicit-any
const seqToArray = (seq: any): any[] => {
  const arr = [];
  for (let i = 0; i < seq.length; i++) {
    arr.push(seq[i]);
  }
  return arr;
};

// deno-lint-ignore no-explicit-any
const toNumber = (bn: any): number => {
  if (bn && typeof bn.toNumber === 'function') {
    return bn.toNumber();
  }
  return bn;
};


// ============================================================================
// TypeScript Type Definitions (JSON representation)
// ============================================================================

export interface Color {
  h: number;
  s: number;
  l: number;
}

export type Mood = 'Vibrant' | 'SoftMuted' | 'Pastel' | 'DeepJewel' | 'Earth' | 'Neon' | 'Custom';

export type Harmony = 'Complementary' | 'Triadic' | 'Analogous' | 'SplitComplement' | 'Square' | 'Custom';

export interface Model {
  baseHue: number;
  mood: Mood;
  harmony: Harmony;
  colors: Color[];
  contrastPair: [number, number];
  adjustmentH: number;
  adjustmentS: number;
  adjustmentL: number;
}

export type Action =
  | { type: 'GeneratePalette'; baseHue: number; mood: Mood; harmony: Harmony; randomSeeds: number[] }
  | { type: 'AdjustColor'; index: number; deltaH: number; deltaS: number; deltaL: number }
  | { type: 'AdjustPalette'; deltaH: number; deltaS: number; deltaL: number }
  | { type: 'SelectContrastPair'; fg: number; bg: number }
  | { type: 'SetColorDirect'; index: number; color: Color }
  | { type: 'RegenerateMood'; mood: Mood; randomSeeds: number[] }
  | { type: 'RegenerateHarmony'; harmony: Harmony; randomSeeds: number[] }
  | { type: 'RandomizeBaseHue'; newBaseHue: number; randomSeeds: number[] };

export interface History {
  past: Model[];
  present: Model;
  future: Model[];
}

// ============================================================================
// Dafny Runtime Types (actual Dafny runtime object shapes)
// ============================================================================

// Base Dafny runtime types
type DafnyInt = InstanceType<typeof BigNumber>;
interface DafnySeq<T = unknown> {
  readonly length: number;
  readonly [index: number]: T;
  toVerbatimString?(asLiteral: boolean): string;
  map<U>(fn: (x: T) => U): U[];
}
type DafnyTuple2<T0, T1> = readonly [T0, T1];

interface DafnyColor {
  readonly is_Color: true;
  readonly dtor_h: DafnyInt;
  readonly dtor_s: DafnyInt;
  readonly dtor_l: DafnyInt;
}

type DafnyMood = { readonly is_Vibrant: true; readonly is_SoftMuted: false; readonly is_Pastel: false; readonly is_DeepJewel: false; readonly is_Earth: false; readonly is_Neon: false; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: true; readonly is_Pastel: false; readonly is_DeepJewel: false; readonly is_Earth: false; readonly is_Neon: false; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: false; readonly is_Pastel: true; readonly is_DeepJewel: false; readonly is_Earth: false; readonly is_Neon: false; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: false; readonly is_Pastel: false; readonly is_DeepJewel: true; readonly is_Earth: false; readonly is_Neon: false; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: false; readonly is_Pastel: false; readonly is_DeepJewel: false; readonly is_Earth: true; readonly is_Neon: false; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: false; readonly is_Pastel: false; readonly is_DeepJewel: false; readonly is_Earth: false; readonly is_Neon: true; readonly is_Custom: false } | { readonly is_Vibrant: false; readonly is_SoftMuted: false; readonly is_Pastel: false; readonly is_DeepJewel: false; readonly is_Earth: false; readonly is_Neon: false; readonly is_Custom: true };

type DafnyHarmony = { readonly is_Complementary: true; readonly is_Triadic: false; readonly is_Analogous: false; readonly is_SplitComplement: false; readonly is_Square: false; readonly is_Custom: false } | { readonly is_Complementary: false; readonly is_Triadic: true; readonly is_Analogous: false; readonly is_SplitComplement: false; readonly is_Square: false; readonly is_Custom: false } | { readonly is_Complementary: false; readonly is_Triadic: false; readonly is_Analogous: true; readonly is_SplitComplement: false; readonly is_Square: false; readonly is_Custom: false } | { readonly is_Complementary: false; readonly is_Triadic: false; readonly is_Analogous: false; readonly is_SplitComplement: true; readonly is_Square: false; readonly is_Custom: false } | { readonly is_Complementary: false; readonly is_Triadic: false; readonly is_Analogous: false; readonly is_SplitComplement: false; readonly is_Square: true; readonly is_Custom: false } | { readonly is_Complementary: false; readonly is_Triadic: false; readonly is_Analogous: false; readonly is_SplitComplement: false; readonly is_Square: false; readonly is_Custom: true };

interface DafnyModel {
  readonly is_Model: true;
  readonly dtor_baseHue: DafnyInt;
  readonly dtor_mood: DafnyMood;
  readonly dtor_harmony: DafnyHarmony;
  readonly dtor_colors: DafnySeq<DafnyColor>;
  readonly dtor_contrastPair: DafnyTuple2<DafnyInt, DafnyInt>;
  readonly dtor_adjustmentH: DafnyInt;
  readonly dtor_adjustmentS: DafnyInt;
  readonly dtor_adjustmentL: DafnyInt;
}

type DafnyAction = { readonly is_GeneratePalette: true; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_baseHue: DafnyInt; readonly dtor_mood: DafnyMood; readonly dtor_harmony: DafnyHarmony; readonly dtor_randomSeeds: DafnySeq<DafnyInt> } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: true; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_index: DafnyInt; readonly dtor_deltaH: DafnyInt; readonly dtor_deltaS: DafnyInt; readonly dtor_deltaL: DafnyInt } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: true; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_deltaH: DafnyInt; readonly dtor_deltaS: DafnyInt; readonly dtor_deltaL: DafnyInt } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: true; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_fg: DafnyInt; readonly dtor_bg: DafnyInt } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: true; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_index: DafnyInt; readonly dtor_color: DafnyColor } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: true; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: false; readonly dtor_mood: DafnyMood; readonly dtor_randomSeeds: DafnySeq<DafnyInt> } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: true; readonly is_RandomizeBaseHue: false; readonly dtor_harmony: DafnyHarmony; readonly dtor_randomSeeds: DafnySeq<DafnyInt> } | { readonly is_GeneratePalette: false; readonly is_AdjustColor: false; readonly is_AdjustPalette: false; readonly is_SelectContrastPair: false; readonly is_SetColorDirect: false; readonly is_RegenerateMood: false; readonly is_RegenerateHarmony: false; readonly is_RandomizeBaseHue: true; readonly dtor_newBaseHue: DafnyInt; readonly dtor_randomSeeds: DafnySeq<DafnyInt> };

interface DafnyHistory {
  readonly is_History: true;
  readonly dtor_past: DafnySeq<DafnyModel>;
  readonly dtor_present: DafnyModel;
  readonly dtor_future: DafnySeq<DafnyModel>;
}

// ============================================================================
// Datatype Conversions
// ============================================================================

// deno-lint-ignore no-explicit-any
const colorFromJson = (json: any): DafnyColor => {
  return ColorWheelSpec.Color.create_Color(
    new BigNumber(json.h),
    new BigNumber(json.s),
    new BigNumber(json.l)
  );
};

// deno-lint-ignore no-explicit-any
const colorToJson = (value: any): Color => {
  return {
    h: toNumber(value.dtor_h),
    s: toNumber(value.dtor_s),
    l: toNumber(value.dtor_l)
  };
};

// deno-lint-ignore no-explicit-any
const moodFromJson = (json: any): DafnyMood => {
  switch (json) {
    case 'Vibrant':
      return ColorWheelSpec.Mood.create_Vibrant();
    case 'SoftMuted':
      return ColorWheelSpec.Mood.create_SoftMuted();
    case 'Pastel':
      return ColorWheelSpec.Mood.create_Pastel();
    case 'DeepJewel':
      return ColorWheelSpec.Mood.create_DeepJewel();
    case 'Earth':
      return ColorWheelSpec.Mood.create_Earth();
    case 'Neon':
      return ColorWheelSpec.Mood.create_Neon();
    case 'Custom':
      return ColorWheelSpec.Mood.create_Custom();
    default:
      throw new Error(`Unknown Mood: ${json}`);
  }
};

// deno-lint-ignore no-explicit-any
const moodToJson = (value: any): Mood => {
  if (value.is_Vibrant) {
    return 'Vibrant';
  } else if (value.is_SoftMuted) {
    return 'SoftMuted';
  } else if (value.is_Pastel) {
    return 'Pastel';
  } else if (value.is_DeepJewel) {
    return 'DeepJewel';
  } else if (value.is_Earth) {
    return 'Earth';
  } else if (value.is_Neon) {
    return 'Neon';
  } else if (value.is_Custom) {
    return 'Custom';
  }
  throw new Error('Unknown Mood variant');
};

// deno-lint-ignore no-explicit-any
const harmonyFromJson = (json: any): DafnyHarmony => {
  switch (json) {
    case 'Complementary':
      return ColorWheelSpec.Harmony.create_Complementary();
    case 'Triadic':
      return ColorWheelSpec.Harmony.create_Triadic();
    case 'Analogous':
      return ColorWheelSpec.Harmony.create_Analogous();
    case 'SplitComplement':
      return ColorWheelSpec.Harmony.create_SplitComplement();
    case 'Square':
      return ColorWheelSpec.Harmony.create_Square();
    case 'Custom':
      return ColorWheelSpec.Harmony.create_Custom();
    default:
      throw new Error(`Unknown Harmony: ${json}`);
  }
};

// deno-lint-ignore no-explicit-any
const harmonyToJson = (value: any): Harmony => {
  if (value.is_Complementary) {
    return 'Complementary';
  } else if (value.is_Triadic) {
    return 'Triadic';
  } else if (value.is_Analogous) {
    return 'Analogous';
  } else if (value.is_SplitComplement) {
    return 'SplitComplement';
  } else if (value.is_Square) {
    return 'Square';
  } else if (value.is_Custom) {
    return 'Custom';
  }
  throw new Error('Unknown Harmony variant');
};

// deno-lint-ignore no-explicit-any
const modelFromJson = (json: any): DafnyModel => {
  return ColorWheelSpec.Model.create_Model(
    new BigNumber(json.baseHue),
    moodFromJson(json.mood),
    harmonyFromJson(json.harmony),
    _dafny.Seq.of(...(json.colors || []).map((x: any) => colorFromJson(x))),
    _dafny.Tuple.create_2(new BigNumber(json.contrastPair[0]), new BigNumber(json.contrastPair[1])),
    new BigNumber(json.adjustmentH),
    new BigNumber(json.adjustmentS),
    new BigNumber(json.adjustmentL)
  );
};

// deno-lint-ignore no-explicit-any
const modelToJson = (value: any): Model => {
  return {
    baseHue: toNumber(value.dtor_baseHue),
    mood: moodToJson(value.dtor_mood),
    harmony: harmonyToJson(value.dtor_harmony),
    colors: seqToArray(value.dtor_colors).map((x: any) => colorToJson(x)),
    contrastPair: [toNumber(value.dtor_contrastPair[0]), toNumber(value.dtor_contrastPair[1])],
    adjustmentH: toNumber(value.dtor_adjustmentH),
    adjustmentS: toNumber(value.dtor_adjustmentS),
    adjustmentL: toNumber(value.dtor_adjustmentL)
  };
};

// deno-lint-ignore no-explicit-any
const actionFromJson = (json: any): DafnyAction => {
  switch (json.type) {
    case 'GeneratePalette': {
      return ColorWheelSpec.Action.create_GeneratePalette(
        new BigNumber(json.baseHue),
        moodFromJson(json.mood),
        harmonyFromJson(json.harmony),
        _dafny.Seq.of(...(json.randomSeeds || []).map((x: any) => new BigNumber(x)))
      );
    }
    case 'AdjustColor': {
      return ColorWheelSpec.Action.create_AdjustColor(
        new BigNumber(json.index),
        new BigNumber(json.deltaH),
        new BigNumber(json.deltaS),
        new BigNumber(json.deltaL)
      );
    }
    case 'AdjustPalette': {
      return ColorWheelSpec.Action.create_AdjustPalette(
        new BigNumber(json.deltaH),
        new BigNumber(json.deltaS),
        new BigNumber(json.deltaL)
      );
    }
    case 'SelectContrastPair': {
      return ColorWheelSpec.Action.create_SelectContrastPair(
        new BigNumber(json.fg),
        new BigNumber(json.bg)
      );
    }
    case 'SetColorDirect': {
      return ColorWheelSpec.Action.create_SetColorDirect(
        new BigNumber(json.index),
        colorFromJson(json.color)
      );
    }
    case 'RegenerateMood': {
      return ColorWheelSpec.Action.create_RegenerateMood(
        moodFromJson(json.mood),
        _dafny.Seq.of(...(json.randomSeeds || []).map((x: any) => new BigNumber(x)))
      );
    }
    case 'RegenerateHarmony': {
      return ColorWheelSpec.Action.create_RegenerateHarmony(
        harmonyFromJson(json.harmony),
        _dafny.Seq.of(...(json.randomSeeds || []).map((x: any) => new BigNumber(x)))
      );
    }
    case 'RandomizeBaseHue': {
      return ColorWheelSpec.Action.create_RandomizeBaseHue(
        new BigNumber(json.newBaseHue),
        _dafny.Seq.of(...(json.randomSeeds || []).map((x: any) => new BigNumber(x)))
      );
    }
    default:
      throw new Error(`Unknown Action type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const actionToJson = (value: any): Action => {
  if (value.is_GeneratePalette) {
    return {
      type: 'GeneratePalette',
      baseHue: toNumber(value.dtor_baseHue),
      mood: moodToJson(value.dtor_mood),
      harmony: harmonyToJson(value.dtor_harmony),
      randomSeeds: seqToArray(value.dtor_randomSeeds).map((x: any) => toNumber(x))
    };
  } else if (value.is_AdjustColor) {
    return {
      type: 'AdjustColor',
      index: toNumber(value.dtor_index),
      deltaH: toNumber(value.dtor_deltaH),
      deltaS: toNumber(value.dtor_deltaS),
      deltaL: toNumber(value.dtor_deltaL)
    };
  } else if (value.is_AdjustPalette) {
    return {
      type: 'AdjustPalette',
      deltaH: toNumber(value.dtor_deltaH),
      deltaS: toNumber(value.dtor_deltaS),
      deltaL: toNumber(value.dtor_deltaL)
    };
  } else if (value.is_SelectContrastPair) {
    return {
      type: 'SelectContrastPair',
      fg: toNumber(value.dtor_fg),
      bg: toNumber(value.dtor_bg)
    };
  } else if (value.is_SetColorDirect) {
    return {
      type: 'SetColorDirect',
      index: toNumber(value.dtor_index),
      color: colorToJson(value.dtor_color)
    };
  } else if (value.is_RegenerateMood) {
    return {
      type: 'RegenerateMood',
      mood: moodToJson(value.dtor_mood),
      randomSeeds: seqToArray(value.dtor_randomSeeds).map((x: any) => toNumber(x))
    };
  } else if (value.is_RegenerateHarmony) {
    return {
      type: 'RegenerateHarmony',
      harmony: harmonyToJson(value.dtor_harmony),
      randomSeeds: seqToArray(value.dtor_randomSeeds).map((x: any) => toNumber(x))
    };
  } else if (value.is_RandomizeBaseHue) {
    return {
      type: 'RandomizeBaseHue',
      newBaseHue: toNumber(value.dtor_newBaseHue),
      randomSeeds: seqToArray(value.dtor_randomSeeds).map((x: any) => toNumber(x))
    };
  }
  throw new Error('Unknown Action variant');
};

// deno-lint-ignore no-explicit-any
const historyFromJson = (json: any): DafnyHistory => {
  return ColorWheelKernel.History.create_History(
    _dafny.Seq.of(...(json.past || []).map((x: any) => modelFromJson(x))),
    modelFromJson(json.present),
    _dafny.Seq.of(...(json.future || []).map((x: any) => modelFromJson(x)))
  );
};

// deno-lint-ignore no-explicit-any
const historyToJson = (value: any): History => {
  return {
    past: seqToArray(value.dtor_past).map((x: any) => modelToJson(x)),
    present: modelToJson(value.dtor_present),
    future: seqToArray(value.dtor_future).map((x: any) => modelToJson(x))
  };
};

// ============================================================================
// API Wrapper
// ============================================================================

interface AppInternal { _dafny: unknown; ColorWheelSpec: unknown; ColorWheelKernel: unknown; AppCore: unknown; }

const App = {
  // Color constructors
  Color: (h: number, s: number, l: number) => ColorWheelSpec.Color.create_Color(new BigNumber(h), new BigNumber(s), new BigNumber(l)),

  // Mood constructors
  Vibrant: () => ColorWheelSpec.Mood.create_Vibrant(),
  SoftMuted: () => ColorWheelSpec.Mood.create_SoftMuted(),
  Pastel: () => ColorWheelSpec.Mood.create_Pastel(),
  DeepJewel: () => ColorWheelSpec.Mood.create_DeepJewel(),
  Earth: () => ColorWheelSpec.Mood.create_Earth(),
  Neon: () => ColorWheelSpec.Mood.create_Neon(),
  Custom: () => ColorWheelSpec.Mood.create_Custom(),

  // Harmony constructors
  Complementary: () => ColorWheelSpec.Harmony.create_Complementary(),
  Triadic: () => ColorWheelSpec.Harmony.create_Triadic(),
  Analogous: () => ColorWheelSpec.Harmony.create_Analogous(),
  SplitComplement: () => ColorWheelSpec.Harmony.create_SplitComplement(),
  Square: () => ColorWheelSpec.Harmony.create_Square(),

  // Action constructors
  GeneratePalette: (baseHue: number, mood: DafnyMood, harmony: DafnyHarmony, randomSeeds: number[]) => ColorWheelSpec.Action.create_GeneratePalette(new BigNumber(baseHue), mood, harmony, _dafny.Seq.of(...(randomSeeds || []).map((x: any) => new BigNumber(x)))),
  AdjustColor: (index: number, deltaH: number, deltaS: number, deltaL: number) => ColorWheelSpec.Action.create_AdjustColor(new BigNumber(index), new BigNumber(deltaH), new BigNumber(deltaS), new BigNumber(deltaL)),
  AdjustPalette: (deltaH: number, deltaS: number, deltaL: number) => ColorWheelSpec.Action.create_AdjustPalette(new BigNumber(deltaH), new BigNumber(deltaS), new BigNumber(deltaL)),
  SelectContrastPair: (fg: number, bg: number) => ColorWheelSpec.Action.create_SelectContrastPair(new BigNumber(fg), new BigNumber(bg)),
  SetColorDirect: (index: number, color: DafnyColor) => ColorWheelSpec.Action.create_SetColorDirect(new BigNumber(index), color),
  RegenerateMood: (mood: DafnyMood, randomSeeds: number[]) => ColorWheelSpec.Action.create_RegenerateMood(mood, _dafny.Seq.of(...(randomSeeds || []).map((x: any) => new BigNumber(x)))),
  RegenerateHarmony: (harmony: DafnyHarmony, randomSeeds: number[]) => ColorWheelSpec.Action.create_RegenerateHarmony(harmony, _dafny.Seq.of(...(randomSeeds || []).map((x: any) => new BigNumber(x)))),
  RandomizeBaseHue: (newBaseHue: number, randomSeeds: number[]) => ColorWheelSpec.Action.create_RandomizeBaseHue(new BigNumber(newBaseHue), _dafny.Seq.of(...(randomSeeds || []).map((x: any) => new BigNumber(x)))),

  // Model accessors
  GetBaseHue: (m: DafnyModel) => toNumber(m.dtor_baseHue),
  GetMood: (m: DafnyModel) => moodToJson(m.dtor_mood),
  GetHarmony: (m: DafnyModel) => harmonyToJson(m.dtor_harmony),
  GetColors: (m: DafnyModel) => seqToArray(m.dtor_colors).map((x: any) => colorToJson(x)),
  GetContrastPair: (m: DafnyModel) => [toNumber(m.dtor_contrastPair[0]), toNumber(m.dtor_contrastPair[1])],
  GetAdjustmentH: (m: DafnyModel) => toNumber(m.dtor_adjustmentH),
  GetAdjustmentS: (m: DafnyModel) => toNumber(m.dtor_adjustmentS),
  GetAdjustmentL: (m: DafnyModel) => toNumber(m.dtor_adjustmentL),

  // AppCore functions
  Init: (): DafnyHistory => AppCore.__default.Init(),
  Dispatch: (h: DafnyHistory, a: DafnyAction): DafnyHistory => AppCore.__default.Dispatch(h, a),
  Preview: (h: DafnyHistory, a: DafnyAction): DafnyHistory => AppCore.__default.Preview(h, a),
  CommitFrom: (h: DafnyHistory, baseline: DafnyModel): DafnyHistory => AppCore.__default.CommitFrom(h, baseline),
  Undo: (h: DafnyHistory): DafnyHistory => AppCore.__default.Undo(h),
  Redo: (h: DafnyHistory): DafnyHistory => AppCore.__default.Redo(h),
  Present: (h: DafnyHistory): DafnyModel => AppCore.__default.Present(h),
  CanUndo: (h: DafnyHistory): boolean => AppCore.__default.CanUndo(h),
  CanRedo: (h: DafnyHistory): boolean => AppCore.__default.CanRedo(h),

  // Conversion functions
  colorToJson,
  colorFromJson,
  moodToJson,
  moodFromJson,
  harmonyToJson,
  harmonyFromJson,
  modelToJson,
  modelFromJson,
  actionToJson,
  actionFromJson,
  historyToJson,
  historyFromJson,

  // Internal modules for custom extensions
  _internal: { _dafny, ColorWheelSpec, ColorWheelKernel, AppCore } as AppInternal,
};

export default App;
