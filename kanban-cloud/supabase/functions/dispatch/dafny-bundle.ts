// Generated by dafny2js
// DO NOT EDIT - regenerate with: ./compile.sh

import BigNumber from 'https://esm.sh/bignumber.js@9.1.2';

BigNumber.config({ MODULO_MODE: BigNumber.EUCLID });


// Dafny runtime mock for require
const require = (mod: string) => {
  if (mod === 'bignumber.js') return BigNumber;
  throw new Error(`Unknown module: ${mod}`);
};

// deno-lint-ignore no-unused-vars
const exports = {};
// deno-lint-ignore no-unused-vars
const module = { exports };

// Evaluate Dafny code
const initDafny = new Function('require', 'exports', 'module', `
// Dafny program KanbanEffectStateMachine.dfy compiled into JavaScript
// Copyright by the contributors to the Dafny Project
// SPDX-License-Identifier: MIT

const BigNumber = require('bignumber.js');
BigNumber.config({ MODULO_MODE: BigNumber.EUCLID })
let _dafny = (function() {
  let \$module = {};
  \$module.areEqual = function(a, b) {
    if (typeof a === 'string' && b instanceof _dafny.Seq) {
      // Seq.equals(string) works as expected,
      // and the catch-all else block handles that direction.
      // But the opposite direction doesn't work; handle it here.
      return b.equals(a);
    } else if (typeof a === 'number' && BigNumber.isBigNumber(b)) {
      // This conditional would be correct even without the \`typeof a\` part,
      // but in most cases it's probably faster to short-circuit on a \`typeof\`
      // than to call \`isBigNumber\`. (But it remains to properly test this.)
      return b.isEqualTo(a);
    } else if (typeof a !== 'object' || a === null || b === null) {
      return a === b;
    } else if (BigNumber.isBigNumber(a)) {
      return a.isEqualTo(b);
    } else if (a._tname !== undefined || (Array.isArray(a) && a.constructor.name == "Array")) {
      return a === b;  // pointer equality
    } else {
      return a.equals(b);  // value-type equality
    }
  }
  \$module.toString = function(a) {
    if (a === null) {
      return "null";
    } else if (typeof a === "number") {
      return a.toFixed();
    } else if (BigNumber.isBigNumber(a)) {
      return a.toFixed();
    } else if (a._tname !== undefined) {
      return a._tname;
    } else {
      return a.toString();
    }
  }
  \$module.escapeCharacter = function(cp) {
    let s = String.fromCodePoint(cp.value)
    switch (s) {
      case '\\n': return "\\\\n";
      case '\\r': return "\\\\r";
      case '\\t': return "\\\\t";
      case '\\0': return "\\\\0";
      case '\\'': return "\\\\'";
      case '\\"': return "\\\\\\"";
      case '\\\\': return "\\\\\\\\";
      default: return s;
    };
  }
  \$module.NewObject = function() {
    return { _tname: "object" };
  }
  \$module.InstanceOfTrait = function(obj, trait) {
    return obj._parentTraits !== undefined && obj._parentTraits().includes(trait);
  }
  \$module.Rtd_bool = class {
    static get Default() { return false; }
  }
  \$module.Rtd_char = class {
    static get Default() { return 'D'; }  // See CharType.DefaultValue in Dafny source code
  }
  \$module.Rtd_codepoint = class {
    static get Default() { return new _dafny.CodePoint('D'.codePointAt(0)); }
  }
  \$module.Rtd_int = class {
    static get Default() { return BigNumber(0); }
  }
  \$module.Rtd_number = class {
    static get Default() { return 0; }
  }
  \$module.Rtd_ref = class {
    static get Default() { return null; }
  }
  \$module.Rtd_array = class {
    static get Default() { return []; }
  }
  \$module.ZERO = new BigNumber(0);
  \$module.ONE = new BigNumber(1);
  \$module.NUMBER_LIMIT = new BigNumber(0x20).multipliedBy(0x1000000000000);  // 2^53
  \$module.Tuple = class Tuple extends Array {
    constructor(...elems) {
      super(...elems);
    }
    toString() {
      return "(" + arrayElementsToString(this) + ")";
    }
    equals(other) {
      if (this === other) {
        return true;
      }
      for (let i = 0; i < this.length; i++) {
        if (!_dafny.areEqual(this[i], other[i])) {
          return false;
        }
      }
      return true;
    }
    static Default(...values) {
      return Tuple.of(...values);
    }
    static Rtd(...rtdArgs) {
      return {
        Default: Tuple.from(rtdArgs, rtd => rtd.Default)
      };
    }
  }
  \$module.Set = class Set extends Array {
    constructor() {
      super();
    }
    static get Default() {
      return Set.Empty;
    }
    toString() {
      return "{" + arrayElementsToString(this) + "}";
    }
    static get Empty() {
      if (this._empty === undefined) {
        this._empty = new Set();
      }
      return this._empty;
    }
    static fromElements(...elmts) {
      let s = new Set();
      for (let k of elmts) {
        s.add(k);
      }
      return s;
    }
    contains(k) {
      for (let i = 0; i < this.length; i++) {
        if (_dafny.areEqual(this[i], k)) {
          return true;
        }
      }
      return false;
    }
    add(k) {  // mutates the Set; use only during construction
      if (!this.contains(k)) {
        this.push(k);
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.length !== other.length) {
        return false;
      }
      for (let e of this) {
        if (!other.contains(e)) {
          return false;
        }
      }
      return true;
    }
    get Elements() {
      return this;
    }
    Union(that) {
      if (this.length === 0) {
        return that;
      } else if (that.length === 0) {
        return this;
      } else {
        let s = Set.of(...this);
        for (let k of that) {
          s.add(k);
        }
        return s;
      }
    }
    Intersect(that) {
      if (this.length === 0) {
        return this;
      } else if (that.length === 0) {
        return that;
      } else {
        let s = new Set();
        for (let k of this) {
          if (that.contains(k)) {
            s.push(k);
          }
        }
        return s;
      }
    }
    Difference(that) {
      if (this.length == 0 || that.length == 0) {
        return this;
      } else {
        let s = new Set();
        for (let k of this) {
          if (!that.contains(k)) {
            s.push(k);
          }
        }
        return s;
      }
    }
    IsDisjointFrom(that) {
      for (let k of this) {
        if (that.contains(k)) {
          return false;
        }
      }
      return true;
    }
    IsSubsetOf(that) {
      if (that.length < this.length) {
        return false;
      }
      for (let k of this) {
        if (!that.contains(k)) {
          return false;
        }
      }
      return true;
    }
    IsProperSubsetOf(that) {
      if (that.length <= this.length) {
        return false;
      }
      for (let k of this) {
        if (!that.contains(k)) {
          return false;
        }
      }
      return true;
    }
    get AllSubsets() {
      return this.AllSubsets_();
    }
    *AllSubsets_() {
      // Start by putting all set elements into a list, but don't include null
      let elmts = Array.of(...this);
      let n = elmts.length;
      let which = new Array(n);
      which.fill(false);
      let a = [];
      while (true) {
        yield Set.of(...a);
        // "add 1" to "which", as if doing a carry chain.  For every digit changed, change the membership of the corresponding element in "a".
        let i = 0;
        for (; i < n && which[i]; i++) {
          which[i] = false;
          // remove elmts[i] from a
          for (let j = 0; j < a.length; j++) {
            if (_dafny.areEqual(a[j], elmts[i])) {
              // move the last element of a into slot j
              a[j] = a[-1];
              a.pop();
              break;
            }
          }
        }
        if (i === n) {
          // we have cycled through all the subsets
          break;
        }
        which[i] = true;
        a.push(elmts[i]);
      }
    }
  }
  \$module.MultiSet = class MultiSet extends Array {
    constructor() {
      super();
    }
    static get Default() {
      return MultiSet.Empty;
    }
    toString() {
      let s = "multiset{";
      let sep = "";
      for (let e of this) {
        let [k, n] = e;
        let ks = _dafny.toString(k);
        while (!n.isZero()) {
          n = n.minus(1);
          s += sep + ks;
          sep = ", ";
        }
      }
      s += "}";
      return s;
    }
    static get Empty() {
      if (this._empty === undefined) {
        this._empty = new MultiSet();
      }
      return this._empty;
    }
    static fromElements(...elmts) {
      let s = new MultiSet();
      for (let e of elmts) {
        s.add(e, _dafny.ONE);
      }
      return s;
    }
    static FromArray(arr) {
      let s = new MultiSet();
      for (let e of arr) {
        s.add(e, _dafny.ONE);
      }
      return s;
    }
    cardinality() {
      let c = _dafny.ZERO;
      for (let e of this) {
        let [k, n] = e;
        c = c.plus(n);
      }
      return c;
    }
    clone() {
      let s = new MultiSet();
      for (let e of this) {
        let [k, n] = e;
        s.push([k, n]);  // make sure to create a new array [k, n] here
      }
      return s;
    }
    findIndex(k) {
      for (let i = 0; i < this.length; i++) {
        if (_dafny.areEqual(this[i][0], k)) {
          return i;
        }
      }
      return this.length;
    }
    get(k) {
      let i = this.findIndex(k);
      if (i === this.length) {
        return _dafny.ZERO;
      } else {
        return this[i][1];
      }
    }
    contains(k) {
      return !this.get(k).isZero();
    }
    add(k, n) {
      let i = this.findIndex(k);
      if (i === this.length) {
        this.push([k, n]);
      } else {
        let m = this[i][1];
        this[i] = [k, m.plus(n)];
      }
    }
    update(k, n) {
      let i = this.findIndex(k);
      if (i < this.length && this[i][1].isEqualTo(n)) {
        return this;
      } else if (i === this.length && n.isZero()) {
        return this;
      } else if (i === this.length) {
        let m = this.slice();
        m.push([k, n]);
        return m;
      } else {
        let m = this.slice();
        m[i] = [k, n];
        return m;
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      }
      for (let e of this) {
        let [k, n] = e;
        let m = other.get(k);
        if (!n.isEqualTo(m)) {
          return false;
        }
      }
      return this.cardinality().isEqualTo(other.cardinality());
    }
    get Elements() {
      return this.Elements_();
    }
    *Elements_() {
      for (let i = 0; i < this.length; i++) {
        let [k, n] = this[i];
        while (!n.isZero()) {
          yield k;
          n = n.minus(1);
        }
      }
    }
    get UniqueElements() {
      return this.UniqueElements_();
    }
    *UniqueElements_() {
      for (let e of this) {
        let [k, n] = e;
        if (!n.isZero()) {
          yield k;
        }
      }
    }
    Union(that) {
      if (this.length === 0) {
        return that;
      } else if (that.length === 0) {
        return this;
      } else {
        let s = this.clone();
        for (let e of that) {
          let [k, n] = e;
          s.add(k, n);
        }
        return s;
      }
    }
    Intersect(that) {
      if (this.length === 0) {
        return this;
      } else if (that.length === 0) {
        return that;
      } else {
        let s = new MultiSet();
        for (let e of this) {
          let [k, n] = e;
          let m = that.get(k);
          if (!m.isZero()) {
            s.push([k, m.isLessThan(n) ? m : n]);
          }
        }
        return s;
      }
    }
    Difference(that) {
      if (this.length === 0 || that.length === 0) {
        return this;
      } else {
        let s = new MultiSet();
        for (let e of this) {
          let [k, n] = e;
          let d = n.minus(that.get(k));
          if (d.isGreaterThan(0)) {
            s.push([k, d]);
          }
        }
        return s;
      }
    }
    IsDisjointFrom(that) {
      let intersection = this.Intersect(that);
      return intersection.cardinality().isZero();
    }
    IsSubsetOf(that) {
      for (let e of this) {
        let [k, n] = e;
        let m = that.get(k);
        if (!n.isLessThanOrEqualTo(m)) {
          return false;
        }
      }
      return true;
    }
    IsProperSubsetOf(that) {
      return this.IsSubsetOf(that) && this.cardinality().isLessThan(that.cardinality());
    }
  }
  \$module.CodePoint = class CodePoint {
    constructor(value) {
      this.value = value
    }
    equals(other) {
      if (this === other) {
        return true;
      }
      return this.value === other.value
    }
    isLessThan(other) {
      return this.value < other.value
    }
    isLessThanOrEqual(other) {
      return this.value <= other.value
    }
    toString() {
      return "'" + \$module.escapeCharacter(this) + "'";
    }
    static isCodePoint(i) {
      return (
        (_dafny.ZERO.isLessThanOrEqualTo(i) && i.isLessThan(new BigNumber(0xD800))) ||
        (new BigNumber(0xE000).isLessThanOrEqualTo(i) && i.isLessThan(new BigNumber(0x11_0000))))
    }
  }
  \$module.Seq = class Seq extends Array {
    constructor(...elems) {
      super(...elems);
    }
    static get Default() {
      return Seq.of();
    }
    static Create(n, init) {
      return Seq.from({length: n}, (_, i) => init(new BigNumber(i)));
    }
    static UnicodeFromString(s) {
      return new Seq(...([...s].map(c => new _dafny.CodePoint(c.codePointAt(0)))))
    }
    toString() {
      return "[" + arrayElementsToString(this) + "]";
    }
    toVerbatimString(asLiteral) {
      if (asLiteral) {
        return '"' + this.map(c => _dafny.escapeCharacter(c)).join("") + '"';
      } else {
        return this.map(c => String.fromCodePoint(c.value)).join("");
      }
    }
    static update(s, i, v) {
      if (typeof s === "string") {
        let p = s.slice(0, i);
        let q = s.slice(i.toNumber() + 1);
        return p.concat(v, q);
      } else {
        let t = s.slice();
        t[i] = v;
        return t;
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.length !== other.length) {
        return false;
      }
      for (let i = 0; i < this.length; i++) {
        if (!_dafny.areEqual(this[i], other[i])) {
          return false;
        }
      }
      return true;
    }
    static contains(s, k) {
      if (typeof s === "string") {
        return s.includes(k);
      } else {
        for (let x of s) {
          if (_dafny.areEqual(x, k)) {
            return true;
          }
        }
        return false;
      }
    }
    get Elements() {
      return this;
    }
    get UniqueElements() {
      return _dafny.Set.fromElements(...this);
    }
    static Concat(a, b) {
      if (typeof a === "string" || typeof b === "string") {
        // string concatenation, so make sure both operands are strings before concatenating
        if (typeof a !== "string") {
          // a must be a Seq
          a = a.join("");
        }
        if (typeof b !== "string") {
          // b must be a Seq
          b = b.join("");
        }
        return a + b;
      } else {
        // ordinary concatenation
        let r = Seq.of(...a);
        r.push(...b);
        return r;
      }
    }
    static JoinIfPossible(x) {
      try { return x.join(""); } catch(_error) { return x; }
    }
    static IsPrefixOf(a, b) {
      if (b.length < a.length) {
        return false;
      }
      for (let i = 0; i < a.length; i++) {
        if (!_dafny.areEqual(a[i], b[i])) {
          return false;
        }
      }
      return true;
    }
    static IsProperPrefixOf(a, b) {
      if (b.length <= a.length) {
        return false;
      }
      for (let i = 0; i < a.length; i++) {
        if (!_dafny.areEqual(a[i], b[i])) {
          return false;
        }
      }
      return true;
    }
  }
  \$module.Map = class Map extends Array {
    constructor() {
      super();
    }
    static get Default() {
      return Map.of();
    }
    toString() {
      return "map[" + this.map(maplet => _dafny.toString(maplet[0]) + " := " + _dafny.toString(maplet[1])).join(", ") + "]";
    }
    static get Empty() {
      if (this._empty === undefined) {
        this._empty = new Map();
      }
      return this._empty;
    }
    findIndex(k) {
      for (let i = 0; i < this.length; i++) {
        if (_dafny.areEqual(this[i][0], k)) {
          return i;
        }
      }
      return this.length;
    }
    get(k) {
      let i = this.findIndex(k);
      if (i === this.length) {
        return undefined;
      } else {
        return this[i][1];
      }
    }
    contains(k) {
      return this.findIndex(k) < this.length;
    }
    update(k, v) {
      let m = this.slice();
      m.updateUnsafe(k, v);
      return m;
    }
    // Similar to update, but make the modification in-place.
    // Meant to be used in the map constructor.
    updateUnsafe(k, v) {
      let m = this;
      let i = m.findIndex(k);
      m[i] = [k, v];
      return m;
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.length !== other.length) {
        return false;
      }
      for (let e of this) {
        let [k, v] = e;
        let w = other.get(k);
        if (w === undefined || !_dafny.areEqual(v, w)) {
          return false;
        }
      }
      return true;
    }
    get Keys() {
      let s = new _dafny.Set();
      for (let e of this) {
        let [k, v] = e;
        s.push(k);
      }
      return s;
    }
    get Values() {
      let s = new _dafny.Set();
      for (let e of this) {
        let [k, v] = e;
        s.add(v);
      }
      return s;
    }
    get Items() {
      let s = new _dafny.Set();
      for (let e of this) {
        let [k, v] = e;
        s.push(_dafny.Tuple.of(k, v));
      }
      return s;
    }
    Merge(that) {
      let m = that.slice();
      for (let e of this) {
        let [k, v] = e;
        let i = m.findIndex(k);
        if (i == m.length) {
          m[i] = [k, v];
        }
      }
      return m;
    }
    Subtract(keys) {
      if (this.length === 0 || keys.length === 0) {
        return this;
      }
      let m = new Map();
      for (let e of this) {
        let [k, v] = e;
        if (!keys.contains(k)) {
          m[m.length] = e;
        }
      }
      return m;
    }
  }
  \$module.newArray = function(initValue, ...dims) {
    return { dims: dims, elmts: buildArray(initValue, ...dims) };
  }
  \$module.BigOrdinal = class BigOrdinal {
    static get Default() {
      return _dafny.ZERO;
    }
    static IsLimit(ord) {
      return ord.isZero();
    }
    static IsSucc(ord) {
      return ord.isGreaterThan(0);
    }
    static Offset(ord) {
      return ord;
    }
    static IsNat(ord) {
      return true;  // at run time, every ORDINAL is a natural number
    }
  }
  \$module.BigRational = class BigRational {
    static get ZERO() {
      if (this._zero === undefined) {
        this._zero = new BigRational(_dafny.ZERO);
      }
      return this._zero;
    }
    constructor (n, d) {
      // requires d === undefined || 1 <= d
      this.num = n;
      this.den = d === undefined ? _dafny.ONE : d;
      // invariant 1 <= den || (num == 0 && den == 0)
    }
    static get Default() {
      return _dafny.BigRational.ZERO;
    }
    // We need to deal with the special case \`num == 0 && den == 0\`, because
    // that's what C#'s default struct constructor will produce for BigRational. :(
    // To deal with it, we ignore \`den\` when \`num\` is 0.
    toString() {
      if (this.num.isZero() || this.den.isEqualTo(1)) {
        return this.num.toFixed() + ".0";
      }
      let answer = this.dividesAPowerOf10(this.den);
      if (answer !== undefined) {
        let n = this.num.multipliedBy(answer[0]);
        let log10 = answer[1];
        let sign, digits;
        if (this.num.isLessThan(0)) {
          sign = "-"; digits = n.negated().toFixed();
        } else {
          sign = ""; digits = n.toFixed();
        }
        if (log10 < digits.length) {
          let digitCount = digits.length - log10;
          return sign + digits.slice(0, digitCount) + "." + digits.slice(digitCount);
        } else {
          return sign + "0." + "0".repeat(log10 - digits.length) + digits;
        }
      } else {
        return "(" + this.num.toFixed() + ".0 / " + this.den.toFixed() + ".0)";
      }
    }
    isPowerOf10(x) {
      if (x.isZero()) {
        return undefined;
      }
      let log10 = 0;
      while (true) {  // invariant: x != 0 && x * 10^log10 == old(x)
        if (x.isEqualTo(1)) {
          return log10;
        } else if (x.mod(10).isZero()) {
          log10++;
          x = x.dividedToIntegerBy(10);
        } else {
          return undefined;
        }
      }
    }
    dividesAPowerOf10(i) {
      let factor = _dafny.ONE;
      let log10 = 0;
      if (i.isLessThanOrEqualTo(_dafny.ZERO)) {
        return undefined;
      }

      // invariant: 1 <= i && i * 10^log10 == factor * old(i)
      while (i.mod(10).isZero()) {
        i = i.dividedToIntegerBy(10);
       log10++;
      }

      while (i.mod(5).isZero()) {
        i = i.dividedToIntegerBy(5);
        factor = factor.multipliedBy(2);
        log10++;
      }
      while (i.mod(2).isZero()) {
        i = i.dividedToIntegerBy(2);
        factor = factor.multipliedBy(5);
        log10++;
      }

      if (i.isEqualTo(_dafny.ONE)) {
        return [factor, log10];
      } else {
        return undefined;
      }
    }
    toBigNumber() {
      if (this.num.isZero() || this.den.isEqualTo(1)) {
        return this.num;
      } else if (this.num.isGreaterThan(0)) {
        return this.num.dividedToIntegerBy(this.den);
      } else {
        return this.num.minus(this.den).plus(1).dividedToIntegerBy(this.den);
      }
    }
    isInteger() {
      return this.equals(new _dafny.BigRational(this.toBigNumber(), _dafny.ONE));
    }
    // Returns values such that aa/dd == a and bb/dd == b.
    normalize(b) {
      let a = this;
      let aa, bb, dd;
      if (a.num.isZero()) {
        aa = a.num;
        bb = b.num;
        dd = b.den;
      } else if (b.num.isZero()) {
        aa = a.num;
        dd = a.den;
        bb = b.num;
      } else {
        let gcd = BigNumberGcd(a.den, b.den);
        let xx = a.den.dividedToIntegerBy(gcd);
        let yy = b.den.dividedToIntegerBy(gcd);
        // We now have a == a.num / (xx * gcd) and b == b.num / (yy * gcd).
        aa = a.num.multipliedBy(yy);
        bb = b.num.multipliedBy(xx);
        dd = a.den.multipliedBy(yy);
      }
      return [aa, bb, dd];
    }
    compareTo(that) {
      // simple things first
      let asign = this.num.isZero() ? 0 : this.num.isLessThan(0) ? -1 : 1;
      let bsign = that.num.isZero() ? 0 : that.num.isLessThan(0) ? -1 : 1;
      if (asign < 0 && 0 <= bsign) {
        return -1;
      } else if (asign <= 0 && 0 < bsign) {
        return -1;
      } else if (bsign < 0 && 0 <= asign) {
        return 1;
      } else if (bsign <= 0 && 0 < asign) {
        return 1;
      }
      let [aa, bb, dd] = this.normalize(that);
      if (aa.isLessThan(bb)) {
        return -1;
      } else if (aa.isEqualTo(bb)){
        return 0;
      } else {
        return 1;
      }
    }
    equals(that) {
      return this.compareTo(that) === 0;
    }
    isLessThan(that) {
      return this.compareTo(that) < 0;
    }
    isAtMost(that) {
      return this.compareTo(that) <= 0;
    }
    plus(b) {
      let [aa, bb, dd] = this.normalize(b);
      return new BigRational(aa.plus(bb), dd);
    }
    minus(b) {
      let [aa, bb, dd] = this.normalize(b);
      return new BigRational(aa.minus(bb), dd);
    }
    negated() {
      return new BigRational(this.num.negated(), this.den);
    }
    multipliedBy(b) {
      return new BigRational(this.num.multipliedBy(b.num), this.den.multipliedBy(b.den));
    }
    dividedBy(b) {
      let a = this;
      // Compute the reciprocal of b
      let bReciprocal;
      if (b.num.isGreaterThan(0)) {
        bReciprocal = new BigRational(b.den, b.num);
      } else {
        // this is the case b.num < 0
        bReciprocal = new BigRational(b.den.negated(), b.num.negated());
      }
      return a.multipliedBy(bReciprocal);
    }
  }
  \$module.EuclideanDivisionNumber = function(a, b) {
    if (0 <= a) {
      if (0 <= b) {
        // +a +b: a/b
        return Math.floor(a / b);
      } else {
        // +a -b: -(a/(-b))
        return -Math.floor(a / -b);
      }
    } else {
      if (0 <= b) {
        // -a +b: -((-a-1)/b) - 1
        return -Math.floor((-a-1) / b) - 1;
      } else {
        // -a -b: ((-a-1)/(-b)) + 1
        return Math.floor((-a-1) / -b) + 1;
      }
    }
  }
  \$module.EuclideanDivision = function(a, b) {
    if (a.isGreaterThanOrEqualTo(0)) {
      if (b.isGreaterThanOrEqualTo(0)) {
        // +a +b: a/b
        return a.dividedToIntegerBy(b);
      } else {
        // +a -b: -(a/(-b))
        return a.dividedToIntegerBy(b.negated()).negated();
      }
    } else {
      if (b.isGreaterThanOrEqualTo(0)) {
        // -a +b: -((-a-1)/b) - 1
        return a.negated().minus(1).dividedToIntegerBy(b).negated().minus(1);
      } else {
        // -a -b: ((-a-1)/(-b)) + 1
        return a.negated().minus(1).dividedToIntegerBy(b.negated()).plus(1);
      }
    }
  }
  \$module.EuclideanModuloNumber = function(a, b) {
    let bp = Math.abs(b);
    if (0 <= a) {
      // +a: a % bp
      return a % bp;
    } else {
      // c = ((-a) % bp)
      // -a: bp - c if c > 0
      // -a: 0 if c == 0
      let c = (-a) % bp;
      return c === 0 ? c : bp - c;
    }
  }
  \$module.ShiftLeft = function(b, n) {
    return b.multipliedBy(new BigNumber(2).exponentiatedBy(n));
  }
  \$module.ShiftRight = function(b, n) {
    return b.dividedToIntegerBy(new BigNumber(2).exponentiatedBy(n));
  }
  \$module.RotateLeft = function(b, n, w) {  // truncate(b << n) | (b >> (w - n))
    let x = _dafny.ShiftLeft(b, n).mod(new BigNumber(2).exponentiatedBy(w));
    let y = _dafny.ShiftRight(b, w - n);
    return x.plus(y);
  }
  \$module.RotateRight = function(b, n, w) {  // (b >> n) | truncate(b << (w - n))
    let x = _dafny.ShiftRight(b, n);
    let y = _dafny.ShiftLeft(b, w - n).mod(new BigNumber(2).exponentiatedBy(w));;
    return x.plus(y);
  }
  \$module.BitwiseAnd = function(a, b) {
    let r = _dafny.ZERO;
    const m = _dafny.NUMBER_LIMIT;  // 2^53
    let h = _dafny.ONE;
    while (!a.isZero() && !b.isZero()) {
      let a0 = a.mod(m);
      let b0 = b.mod(m);
      r = r.plus(h.multipliedBy(a0 & b0));
      a = a.dividedToIntegerBy(m);
      b = b.dividedToIntegerBy(m);
      h = h.multipliedBy(m);
    }
    return r;
  }
  \$module.BitwiseOr = function(a, b) {
    let r = _dafny.ZERO;
    const m = _dafny.NUMBER_LIMIT;  // 2^53
    let h = _dafny.ONE;
    while (!a.isZero() && !b.isZero()) {
      let a0 = a.mod(m);
      let b0 = b.mod(m);
      r = r.plus(h.multipliedBy(a0 | b0));
      a = a.dividedToIntegerBy(m);
      b = b.dividedToIntegerBy(m);
      h = h.multipliedBy(m);
    }
    r = r.plus(h.multipliedBy(a | b));
    return r;
  }
  \$module.BitwiseXor = function(a, b) {
    let r = _dafny.ZERO;
    const m = _dafny.NUMBER_LIMIT;  // 2^53
    let h = _dafny.ONE;
    while (!a.isZero() && !b.isZero()) {
      let a0 = a.mod(m);
      let b0 = b.mod(m);
      r = r.plus(h.multipliedBy(a0 ^ b0));
      a = a.dividedToIntegerBy(m);
      b = b.dividedToIntegerBy(m);
      h = h.multipliedBy(m);
    }
    r = r.plus(h.multipliedBy(a | b));
    return r;
  }
  \$module.BitwiseNot = function(a, bits) {
    let r = _dafny.ZERO;
    let h = _dafny.ONE;
    for (let i = 0; i < bits; i++) {
      let bit = a.mod(2);
      if (bit.isZero()) {
        r = r.plus(h);
      }
      a = a.dividedToIntegerBy(2);
      h = h.multipliedBy(2);
    }
    return r;
  }
  \$module.Quantifier = function(vals, frall, pred) {
    for (let u of vals) {
      if (pred(u) !== frall) { return !frall; }
    }
    return frall;
  }
  \$module.PlusChar = function(a, b) {
    return String.fromCharCode(a.charCodeAt(0) + b.charCodeAt(0));
  }
  \$module.UnicodePlusChar = function(a, b) {
    return new _dafny.CodePoint(a.value + b.value);
  }
  \$module.MinusChar = function(a, b) {
    return String.fromCharCode(a.charCodeAt(0) - b.charCodeAt(0));
  }
  \$module.UnicodeMinusChar = function(a, b) {
    return new _dafny.CodePoint(a.value - b.value);
  }
  \$module.AllBooleans = function*() {
    yield false;
    yield true;
  }
  \$module.AllChars = function*() {
    for (let i = 0; i < 0x10000; i++) {
      yield String.fromCharCode(i);
    }
  }
  \$module.AllUnicodeChars = function*() {
    for (let i = 0; i < 0xD800; i++) {
      yield new _dafny.CodePoint(i);
    }
    for (let i = 0xE0000; i < 0x110000; i++) {
      yield new _dafny.CodePoint(i);
    }
  }
  \$module.AllIntegers = function*() {
    yield _dafny.ZERO;
    for (let j = _dafny.ONE;; j = j.plus(1)) {
      yield j;
      yield j.negated();
    }
  }
  \$module.IntegerRange = function*(lo, hi) {
    if (lo === null) {
      while (true) {
        hi = hi.minus(1);
        yield hi;
      }
    } else if (hi === null) {
      while (true) {
        yield lo;
        lo = lo.plus(1);
      }
    } else {
      while (lo.isLessThan(hi)) {
        yield lo;
        lo = lo.plus(1);
      }
    }
  }
  \$module.SingleValue = function*(v) {
    yield v;
  }
  \$module.HaltException = class HaltException extends Error {
    constructor(message) {
      super(message)
    }
  }
  \$module.HandleHaltExceptions = function(f) {
    try {
      f()
    } catch (e) {
      if (e instanceof _dafny.HaltException) {
        process.stdout.write("[Program halted] " + e.message + "\\n")
        process.exitCode = 1
      } else {
        throw e
      }
    }
  }
  \$module.FromMainArguments = function(args) {
    var a = [...args];
    a.splice(0, 2, args[0] + " " + args[1]);
    return a;
  }
  \$module.UnicodeFromMainArguments = function(args) {
    return \$module.FromMainArguments(args).map(_dafny.Seq.UnicodeFromString);
  }
  return \$module;

  // What follows are routines private to the Dafny runtime
  function buildArray(initValue, ...dims) {
    if (dims.length === 0) {
      return initValue;
    } else {
      let a = Array(dims[0].toNumber());
      let b = Array.from(a, (x) => buildArray(initValue, ...dims.slice(1)));
      return b;
    }
  }
  function arrayElementsToString(a) {
    // like \`a.join(", ")\`, but calling _dafny.toString(x) on every element x instead of x.toString()
    let s = "";
    let sep = "";
    for (let x of a) {
      s += sep + _dafny.toString(x);
      sep = ", ";
    }
    return s;
  }
  function BigNumberGcd(a, b){  // gcd of two non-negative BigNumber's
    while (true) {
      if (a.isZero()) {
        return b;
      } else if (b.isZero()) {
        return a;
      }
      if (a.isLessThan(b)) {
        b = b.modulo(a);
      } else {
        a = a.modulo(b);
      }
    }
  }
})();
// Dafny program systemModulePopulator.dfy compiled into JavaScript
let _System = (function() {
  let \$module = {};

  \$module.nat = class nat {
    constructor () {
    }
    static get Default() {
      return _dafny.ZERO;
    }
    static _Is(__source) {
      let _0_x = (__source);
      return (_dafny.ZERO).isLessThanOrEqualTo(_0_x);
    }
  };

  return \$module;
})(); // end of module _System
let KanbanDomain = (function() {
  let \$module = {};

  \$module.__default = class __default {
    constructor () {
      this._tname = "KanbanDomain._default";
    }
    _parentTraits() {
      return [];
    }
    static RejectErr() {
      return KanbanDomain.Err.create_Rejected();
    };
    static NoDupSeq(s) {
      return _dafny.Quantifier(_dafny.IntegerRange(_dafny.ZERO, new BigNumber((s).length)), true, function (_forall_var_0) {
        let _0_i = _forall_var_0;
        return _dafny.Quantifier(_dafny.IntegerRange((_0_i).plus(_dafny.ONE), new BigNumber((s).length)), true, function (_forall_var_1) {
          let _1_j = _forall_var_1;
          return !((((_dafny.ZERO).isLessThanOrEqualTo(_0_i)) && ((_0_i).isLessThan(_1_j))) && ((_1_j).isLessThan(new BigNumber((s).length)))) || (!_dafny.areEqual((s)[_0_i], (s)[_1_j]));
        });
      });
    };
    static AllIds(m) {
      return KanbanDomain.__default.AllIdsHelper((m).dtor_cols, (m).dtor_lanes);
    };
    static AllIdsHelper(cols, lanes) {
      let _0___accumulator = _dafny.Seq.of();
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((cols).length)).isEqualTo(_dafny.ZERO)) {
          return _dafny.Seq.Concat(_0___accumulator, _dafny.Seq.of());
        } else {
          let _1_c = (cols)[_dafny.ZERO];
          let _2_lane = (((lanes).contains(_1_c)) ? ((lanes).get(_1_c)) : (_dafny.Seq.of()));
          _0___accumulator = _dafny.Seq.Concat(_0___accumulator, _2_lane);
          let _in0 = (cols).slice(_dafny.ONE);
          let _in1 = lanes;
          cols = _in0;
          lanes = _in1;
          continue TAIL_CALL_START;
        }
      }
    };
    static OccursInLanes(m, id) {
      return _dafny.Quantifier(((m).dtor_lanes).Keys.Elements, false, function (_exists_var_0) {
        let _0_c = _exists_var_0;
        return (((m).dtor_lanes).contains(_0_c)) && (KanbanDomain.__default.SeqContains(((m).dtor_lanes).get(_0_c), id));
      });
    };
    static CountInLanes(m, id) {
      return KanbanDomain.__default.CountInLanesHelper((m).dtor_cols, (m).dtor_lanes, id);
    };
    static CountInLanesHelper(cols, lanes, id) {
      let _0___accumulator = _dafny.ZERO;
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((cols).length)).isEqualTo(_dafny.ZERO)) {
          return (_dafny.ZERO).plus(_0___accumulator);
        } else {
          let _1_c = (cols)[_dafny.ZERO];
          let _2_lane = (((lanes).contains(_1_c)) ? ((lanes).get(_1_c)) : (_dafny.Seq.of()));
          let _3_here = ((KanbanDomain.__default.SeqContains(_2_lane, id)) ? (_dafny.ONE) : (_dafny.ZERO));
          _0___accumulator = (_0___accumulator).plus(_3_here);
          let _in0 = (cols).slice(_dafny.ONE);
          let _in1 = lanes;
          let _in2 = id;
          cols = _in0;
          lanes = _in1;
          id = _in2;
          continue TAIL_CALL_START;
        }
      }
    };
    static Init() {
      return KanbanDomain.Model.create_Model(_dafny.Seq.of(), _dafny.Map.Empty.slice(), _dafny.Map.Empty.slice(), _dafny.Map.Empty.slice(), _dafny.ZERO);
    };
    static Get(mp, k, d) {
      if ((mp).contains(k)) {
        return (mp).get(k);
      } else {
        return d;
      }
    };
    static Lane(m, c) {
      return KanbanDomain.__default.Get((m).dtor_lanes, c, _dafny.Seq.of());
    };
    static Wip(m, c) {
      return KanbanDomain.__default.Get((m).dtor_wip, c, _dafny.ZERO);
    };
    static SeqContains(s, x) {
      return _dafny.Quantifier(_dafny.IntegerRange(_dafny.ZERO, new BigNumber((s).length)), false, function (_exists_var_0) {
        let _0_i = _exists_var_0;
        return (((_dafny.ZERO).isLessThanOrEqualTo(_0_i)) && ((_0_i).isLessThan(new BigNumber((s).length)))) && (_dafny.areEqual((s)[_0_i], x));
      });
    };
    static RemoveFirst(s, x) {
      let _0___accumulator = _dafny.Seq.of();
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((s).length)).isEqualTo(_dafny.ZERO)) {
          return _dafny.Seq.Concat(_0___accumulator, _dafny.Seq.of());
        } else if (_dafny.areEqual((s)[_dafny.ZERO], x)) {
          return _dafny.Seq.Concat(_0___accumulator, (s).slice(_dafny.ONE));
        } else {
          _0___accumulator = _dafny.Seq.Concat(_0___accumulator, _dafny.Seq.of((s)[_dafny.ZERO]));
          let _in0 = (s).slice(_dafny.ONE);
          let _in1 = x;
          s = _in0;
          x = _in1;
          continue TAIL_CALL_START;
        }
      }
    };
    static InsertAt(s, i, x) {
      return _dafny.Seq.Concat(_dafny.Seq.Concat((s).slice(0, i), _dafny.Seq.of(x)), (s).slice(i));
    };
    static IndexOf(s, x) {
      if ((new BigNumber((s).length)).isEqualTo(_dafny.ZERO)) {
        return new BigNumber(-1);
      } else if (_dafny.areEqual((s)[_dafny.ZERO], x)) {
        return _dafny.ZERO;
      } else {
        let _0_j = KanbanDomain.__default.IndexOf((s).slice(_dafny.ONE), x);
        if ((_0_j).isLessThan(_dafny.ZERO)) {
          return new BigNumber(-1);
        } else {
          return (_0_j).plus(_dafny.ONE);
        }
      }
    };
    static ClampPos(pos, n) {
      if ((pos).isLessThanOrEqualTo(_dafny.ZERO)) {
        return _dafny.ZERO;
      } else if ((n).isLessThanOrEqualTo(pos)) {
        return (n);
      } else {
        return (pos);
      }
    };
    static PosFromPlace(lane, p) {
      let _source0 = p;
      {
        if (_source0.is_AtEnd) {
          return new BigNumber((lane).length);
        }
      }
      {
        if (_source0.is_Before) {
          let _0_a = (_source0).anchor;
          let _1_i = KanbanDomain.__default.IndexOf(lane, _0_a);
          if ((_1_i).isLessThan(_dafny.ZERO)) {
            return new BigNumber(-1);
          } else {
            return _1_i;
          }
        }
      }
      {
        let _2_a = (_source0).anchor;
        let _3_i = KanbanDomain.__default.IndexOf(lane, _2_a);
        if ((_3_i).isLessThan(_dafny.ZERO)) {
          return new BigNumber(-1);
        } else {
          return (_3_i).plus(_dafny.ONE);
        }
      }
    };
    static TryStep(m, a) {
      let _source0 = a;
      {
        if (_source0.is_NoOp) {
          return KanbanDomain.Result.create_Ok(m);
        }
      }
      {
        if (_source0.is_AddColumn) {
          let _0_col = (_source0).col;
          let _1_limit = (_source0).limit;
          if (_dafny.Seq.contains((m).dtor_cols, _0_col)) {
            return KanbanDomain.Result.create_Ok(m);
          } else {
            return KanbanDomain.Result.create_Ok(KanbanDomain.Model.create_Model(_dafny.Seq.Concat((m).dtor_cols, _dafny.Seq.of(_0_col)), ((m).dtor_lanes).update(_0_col, _dafny.Seq.of()), ((m).dtor_wip).update(_0_col, _1_limit), (m).dtor_cards, (m).dtor_nextId));
          }
        }
      }
      {
        if (_source0.is_SetWip) {
          let _2_col = (_source0).col;
          let _3_limit = (_source0).limit;
          if (!(_dafny.Seq.contains((m).dtor_cols, _2_col))) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_MissingColumn());
          } else if ((_3_limit).isLessThan(new BigNumber((KanbanDomain.__default.Lane(m, _2_col)).length))) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_WipExceeded());
          } else {
            return KanbanDomain.Result.create_Ok(KanbanDomain.Model.create_Model((m).dtor_cols, (m).dtor_lanes, ((m).dtor_wip).update(_2_col, _3_limit), (m).dtor_cards, (m).dtor_nextId));
          }
        }
      }
      {
        if (_source0.is_AddCard) {
          let _4_col = (_source0).col;
          let _5_title = (_source0).title;
          if (!(_dafny.Seq.contains((m).dtor_cols, _4_col))) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_MissingColumn());
          } else if ((KanbanDomain.__default.Wip(m, _4_col)).isLessThan((new BigNumber((KanbanDomain.__default.Lane(m, _4_col)).length)).plus(_dafny.ONE))) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_WipExceeded());
          } else {
            let _6_id = (m).dtor_nextId;
            return KanbanDomain.Result.create_Ok(KanbanDomain.Model.create_Model((m).dtor_cols, ((m).dtor_lanes).update(_4_col, _dafny.Seq.Concat(KanbanDomain.__default.Lane(m, _4_col), _dafny.Seq.of(_6_id))), (m).dtor_wip, ((m).dtor_cards).update(_6_id, _5_title), ((m).dtor_nextId).plus(_dafny.ONE)));
          }
        }
      }
      {
        if (_source0.is_EditTitle) {
          let _7_id = (_source0).id;
          let _8_title = (_source0).title;
          if (!(((m).dtor_cards).contains(_7_id))) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_MissingCard());
          } else {
            return KanbanDomain.Result.create_Ok(KanbanDomain.Model.create_Model((m).dtor_cols, (m).dtor_lanes, (m).dtor_wip, ((m).dtor_cards).update(_7_id, _8_title), (m).dtor_nextId));
          }
        }
      }
      {
        let _9_id = (_source0).id;
        let _10_toCol = (_source0).toCol;
        let _11_place = (_source0).place;
        if (!(((m).dtor_cards).contains(_9_id))) {
          return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_MissingCard());
        } else if (!(_dafny.Seq.contains((m).dtor_cols, _10_toCol))) {
          return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_MissingColumn());
        } else if ((KanbanDomain.__default.Wip(m, _10_toCol)).isLessThan((new BigNumber((KanbanDomain.__default.Lane(m, _10_toCol)).length)).plus(((KanbanDomain.__default.SeqContains(KanbanDomain.__default.Lane(m, _10_toCol), _9_id)) ? (_dafny.ZERO) : (_dafny.ONE))))) {
          return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_WipExceeded());
        } else {
          let _12_lanes1 = function () {
            let _coll0 = new _dafny.Map();
            for (const _compr_0 of ((m).dtor_lanes).Keys.Elements) {
              let _13_c = _compr_0;
              if (((m).dtor_lanes).contains(_13_c)) {
                _coll0.push([_13_c,KanbanDomain.__default.RemoveFirst(((m).dtor_lanes).get(_13_c), _9_id)]);
              }
            }
            return _coll0;
          }();
          let _14_tgt = KanbanDomain.__default.Get(_12_lanes1, _10_toCol, _dafny.Seq.of());
          let _15_pos = KanbanDomain.__default.PosFromPlace(_14_tgt, _11_place);
          if ((_15_pos).isLessThan(_dafny.ZERO)) {
            return KanbanDomain.Result.create_Err(KanbanDomain.Err.create_BadAnchor());
          } else {
            let _16_k = KanbanDomain.__default.ClampPos(_15_pos, new BigNumber((_14_tgt).length));
            let _17_tgt2 = KanbanDomain.__default.InsertAt(_14_tgt, _16_k, _9_id);
            return KanbanDomain.Result.create_Ok(KanbanDomain.Model.create_Model((m).dtor_cols, (_12_lanes1).update(_10_toCol, _17_tgt2), (m).dtor_wip, (m).dtor_cards, (m).dtor_nextId));
          }
        }
      }
    };
    static PlaceAnchor(p) {
      let _source0 = p;
      {
        if (_source0.is_AtEnd) {
          return KanbanDomain.Option.create_None();
        }
      }
      {
        if (_source0.is_Before) {
          let _0_a = (_source0).anchor;
          return KanbanDomain.Option.create_Some(_0_a);
        }
      }
      {
        let _1_a = (_source0).anchor;
        return KanbanDomain.Option.create_Some(_1_a);
      }
    };
    static DegradeIfAnchorMoved(movedId, p) {
      let _source0 = p;
      {
        if (_source0.is_AtEnd) {
          return KanbanDomain.Place.create_AtEnd();
        }
      }
      {
        if (_source0.is_Before) {
          let _0_a = (_source0).anchor;
          if ((_0_a).isEqualTo(movedId)) {
            return KanbanDomain.Place.create_AtEnd();
          } else {
            return p;
          }
        }
      }
      {
        let _1_a = (_source0).anchor;
        if ((_1_a).isEqualTo(movedId)) {
          return KanbanDomain.Place.create_AtEnd();
        } else {
          return p;
        }
      }
    };
    static Rebase(remote, local) {
      let _source0 = _dafny.Tuple.of(remote, local);
      {
        let _00 = (_source0)[0];
        if (_00.is_NoOp) {
          return local;
        }
      }
      {
        let _10 = (_source0)[1];
        if (_10.is_NoOp) {
          return KanbanDomain.Action.create_NoOp();
        }
      }
      {
        let _01 = (_source0)[0];
        if (_01.is_MoveCard) {
          let _0_rid = (_01).id;
          let _11 = (_source0)[1];
          if (_11.is_MoveCard) {
            let _1_lid = (_11).id;
            let _2_ltoCol = (_11).toCol;
            let _3_lplace = (_11).place;
            if ((_0_rid).isEqualTo(_1_lid)) {
              return local;
            } else {
              return KanbanDomain.Action.create_MoveCard(_1_lid, _2_ltoCol, KanbanDomain.__default.DegradeIfAnchorMoved(_0_rid, _3_lplace));
            }
          }
        }
      }
      {
        let _02 = (_source0)[0];
        if (_02.is_EditTitle) {
          let _12 = (_source0)[1];
          if (_12.is_EditTitle) {
            return local;
          }
        }
      }
      {
        let _03 = (_source0)[0];
        if (_03.is_MoveCard) {
          let _4_rid = (_03).id;
          return local;
        }
      }
      {
        let _04 = (_source0)[0];
        if (_04.is_AddColumn) {
          return local;
        }
      }
      {
        let _05 = (_source0)[0];
        if (_05.is_SetWip) {
          return local;
        }
      }
      {
        let _06 = (_source0)[0];
        if (_06.is_AddCard) {
          return local;
        }
      }
      {
        let _07 = (_source0)[0];
        return local;
      }
    };
    static Candidates(m, a) {
      let _source0 = a;
      {
        if (_source0.is_MoveCard) {
          let _0_id = (_source0).id;
          let _1_toCol = (_source0).toCol;
          let _2_place = (_source0).place;
          let _3_lane = KanbanDomain.__default.Lane(m, _1_toCol);
          if (_dafny.areEqual(_2_place, KanbanDomain.Place.create_AtEnd())) {
            return _dafny.Seq.of(KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, KanbanDomain.Place.create_AtEnd()));
          } else if ((new BigNumber((_3_lane).length)).isEqualTo(_dafny.ZERO)) {
            return _dafny.Seq.of(KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, _2_place), KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, KanbanDomain.Place.create_AtEnd()));
          } else {
            let _4_first = (_3_lane)[_dafny.ZERO];
            return _dafny.Seq.of(KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, _2_place), KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, KanbanDomain.Place.create_AtEnd()), KanbanDomain.Action.create_MoveCard(_0_id, _1_toCol, KanbanDomain.Place.create_Before(_4_first)));
          }
        }
      }
      {
        return _dafny.Seq.of(a);
      }
    };
    static RebaseThroughSuffix(suffix, a) {
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((suffix).length)).isEqualTo(_dafny.ZERO)) {
          return a;
        } else {
          let _in0 = (suffix).slice(0, (new BigNumber((suffix).length)).minus(_dafny.ONE));
          let _in1 = KanbanDomain.__default.Rebase((suffix)[(new BigNumber((suffix).length)).minus(_dafny.ONE)], a);
          suffix = _in0;
          a = _in1;
          continue TAIL_CALL_START;
        }
      }
    };
  };

  \$module.Card = class Card {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Card(title) {
      let \$dt = new Card(0);
      \$dt.title = title;
      return \$dt;
    }
    get is_Card() { return this.\$tag === 0; }
    get dtor_title() { return this.title; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Card.Card" + "(" + this.title.toVerbatimString(true) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.title, other.title);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return _dafny.Seq.UnicodeFromString("");
    }
    static Rtd() {
      return class {
        static get Default() {
          return Card.Default();
        }
      };
    }
  }

  \$module.Model = class Model {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Model(cols, lanes, wip, cards, nextId) {
      let \$dt = new Model(0);
      \$dt.cols = cols;
      \$dt.lanes = lanes;
      \$dt.wip = wip;
      \$dt.cards = cards;
      \$dt.nextId = nextId;
      return \$dt;
    }
    get is_Model() { return this.\$tag === 0; }
    get dtor_cols() { return this.cols; }
    get dtor_lanes() { return this.lanes; }
    get dtor_wip() { return this.wip; }
    get dtor_cards() { return this.cards; }
    get dtor_nextId() { return this.nextId; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Model.Model" + "(" + _dafny.toString(this.cols) + ", " + _dafny.toString(this.lanes) + ", " + _dafny.toString(this.wip) + ", " + _dafny.toString(this.cards) + ", " + _dafny.toString(this.nextId) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.cols, other.cols) && _dafny.areEqual(this.lanes, other.lanes) && _dafny.areEqual(this.wip, other.wip) && _dafny.areEqual(this.cards, other.cards) && _dafny.areEqual(this.nextId, other.nextId);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanDomain.Model.create_Model(_dafny.Seq.of(), _dafny.Map.Empty, _dafny.Map.Empty, _dafny.Map.Empty, _dafny.ZERO);
    }
    static Rtd() {
      return class {
        static get Default() {
          return Model.Default();
        }
      };
    }
  }

  \$module.Err = class Err {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_MissingColumn() {
      let \$dt = new Err(0);
      return \$dt;
    }
    static create_MissingCard() {
      let \$dt = new Err(1);
      return \$dt;
    }
    static create_WipExceeded() {
      let \$dt = new Err(2);
      return \$dt;
    }
    static create_BadAnchor() {
      let \$dt = new Err(3);
      return \$dt;
    }
    static create_Rejected() {
      let \$dt = new Err(4);
      return \$dt;
    }
    get is_MissingColumn() { return this.\$tag === 0; }
    get is_MissingCard() { return this.\$tag === 1; }
    get is_WipExceeded() { return this.\$tag === 2; }
    get is_BadAnchor() { return this.\$tag === 3; }
    get is_Rejected() { return this.\$tag === 4; }
    static get AllSingletonConstructors() {
      return this.AllSingletonConstructors_();
    }
    static *AllSingletonConstructors_() {
      yield Err.create_MissingColumn();
      yield Err.create_MissingCard();
      yield Err.create_WipExceeded();
      yield Err.create_BadAnchor();
      yield Err.create_Rejected();
    }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Err.MissingColumn";
      } else if (this.\$tag === 1) {
        return "KanbanDomain.Err.MissingCard";
      } else if (this.\$tag === 2) {
        return "KanbanDomain.Err.WipExceeded";
      } else if (this.\$tag === 3) {
        return "KanbanDomain.Err.BadAnchor";
      } else if (this.\$tag === 4) {
        return "KanbanDomain.Err.Rejected";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1;
      } else if (this.\$tag === 2) {
        return other.\$tag === 2;
      } else if (this.\$tag === 3) {
        return other.\$tag === 3;
      } else if (this.\$tag === 4) {
        return other.\$tag === 4;
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanDomain.Err.create_MissingColumn();
    }
    static Rtd() {
      return class {
        static get Default() {
          return Err.Default();
        }
      };
    }
  }

  \$module.Option = class Option {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_None() {
      let \$dt = new Option(0);
      return \$dt;
    }
    static create_Some(value) {
      let \$dt = new Option(1);
      \$dt.value = value;
      return \$dt;
    }
    get is_None() { return this.\$tag === 0; }
    get is_Some() { return this.\$tag === 1; }
    get dtor_value() { return this.value; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Option.None";
      } else if (this.\$tag === 1) {
        return "KanbanDomain.Option.Some" + "(" + _dafny.toString(this.value) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.value, other.value);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanDomain.Option.create_None();
    }
    static Rtd() {
      return class {
        static get Default() {
          return Option.Default();
        }
      };
    }
  }

  \$module.Place = class Place {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_AtEnd() {
      let \$dt = new Place(0);
      return \$dt;
    }
    static create_Before(anchor) {
      let \$dt = new Place(1);
      \$dt.anchor = anchor;
      return \$dt;
    }
    static create_After(anchor) {
      let \$dt = new Place(2);
      \$dt.anchor = anchor;
      return \$dt;
    }
    get is_AtEnd() { return this.\$tag === 0; }
    get is_Before() { return this.\$tag === 1; }
    get is_After() { return this.\$tag === 2; }
    get dtor_anchor() { return this.anchor; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Place.AtEnd";
      } else if (this.\$tag === 1) {
        return "KanbanDomain.Place.Before" + "(" + _dafny.toString(this.anchor) + ")";
      } else if (this.\$tag === 2) {
        return "KanbanDomain.Place.After" + "(" + _dafny.toString(this.anchor) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.anchor, other.anchor);
      } else if (this.\$tag === 2) {
        return other.\$tag === 2 && _dafny.areEqual(this.anchor, other.anchor);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanDomain.Place.create_AtEnd();
    }
    static Rtd() {
      return class {
        static get Default() {
          return Place.Default();
        }
      };
    }
  }

  \$module.Action = class Action {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_NoOp() {
      let \$dt = new Action(0);
      return \$dt;
    }
    static create_AddColumn(col, limit) {
      let \$dt = new Action(1);
      \$dt.col = col;
      \$dt.limit = limit;
      return \$dt;
    }
    static create_SetWip(col, limit) {
      let \$dt = new Action(2);
      \$dt.col = col;
      \$dt.limit = limit;
      return \$dt;
    }
    static create_AddCard(col, title) {
      let \$dt = new Action(3);
      \$dt.col = col;
      \$dt.title = title;
      return \$dt;
    }
    static create_MoveCard(id, toCol, place) {
      let \$dt = new Action(4);
      \$dt.id = id;
      \$dt.toCol = toCol;
      \$dt.place = place;
      return \$dt;
    }
    static create_EditTitle(id, title) {
      let \$dt = new Action(5);
      \$dt.id = id;
      \$dt.title = title;
      return \$dt;
    }
    get is_NoOp() { return this.\$tag === 0; }
    get is_AddColumn() { return this.\$tag === 1; }
    get is_SetWip() { return this.\$tag === 2; }
    get is_AddCard() { return this.\$tag === 3; }
    get is_MoveCard() { return this.\$tag === 4; }
    get is_EditTitle() { return this.\$tag === 5; }
    get dtor_col() { return this.col; }
    get dtor_limit() { return this.limit; }
    get dtor_title() { return this.title; }
    get dtor_id() { return this.id; }
    get dtor_toCol() { return this.toCol; }
    get dtor_place() { return this.place; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Action.NoOp";
      } else if (this.\$tag === 1) {
        return "KanbanDomain.Action.AddColumn" + "(" + this.col.toVerbatimString(true) + ", " + _dafny.toString(this.limit) + ")";
      } else if (this.\$tag === 2) {
        return "KanbanDomain.Action.SetWip" + "(" + this.col.toVerbatimString(true) + ", " + _dafny.toString(this.limit) + ")";
      } else if (this.\$tag === 3) {
        return "KanbanDomain.Action.AddCard" + "(" + this.col.toVerbatimString(true) + ", " + this.title.toVerbatimString(true) + ")";
      } else if (this.\$tag === 4) {
        return "KanbanDomain.Action.MoveCard" + "(" + _dafny.toString(this.id) + ", " + this.toCol.toVerbatimString(true) + ", " + _dafny.toString(this.place) + ")";
      } else if (this.\$tag === 5) {
        return "KanbanDomain.Action.EditTitle" + "(" + _dafny.toString(this.id) + ", " + this.title.toVerbatimString(true) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.col, other.col) && _dafny.areEqual(this.limit, other.limit);
      } else if (this.\$tag === 2) {
        return other.\$tag === 2 && _dafny.areEqual(this.col, other.col) && _dafny.areEqual(this.limit, other.limit);
      } else if (this.\$tag === 3) {
        return other.\$tag === 3 && _dafny.areEqual(this.col, other.col) && _dafny.areEqual(this.title, other.title);
      } else if (this.\$tag === 4) {
        return other.\$tag === 4 && _dafny.areEqual(this.id, other.id) && _dafny.areEqual(this.toCol, other.toCol) && _dafny.areEqual(this.place, other.place);
      } else if (this.\$tag === 5) {
        return other.\$tag === 5 && _dafny.areEqual(this.id, other.id) && _dafny.areEqual(this.title, other.title);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanDomain.Action.create_NoOp();
    }
    static Rtd() {
      return class {
        static get Default() {
          return Action.Default();
        }
      };
    }
  }

  \$module.Result = class Result {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Ok(value) {
      let \$dt = new Result(0);
      \$dt.value = value;
      return \$dt;
    }
    static create_Err(error) {
      let \$dt = new Result(1);
      \$dt.error = error;
      return \$dt;
    }
    get is_Ok() { return this.\$tag === 0; }
    get is_Err() { return this.\$tag === 1; }
    get dtor_value() { return this.value; }
    get dtor_error() { return this.error; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanDomain.Result.Ok" + "(" + _dafny.toString(this.value) + ")";
      } else if (this.\$tag === 1) {
        return "KanbanDomain.Result.Err" + "(" + _dafny.toString(this.error) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.value, other.value);
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.error, other.error);
      } else  {
        return false; // unexpected
      }
    }
    static Default(_default_T) {
      return KanbanDomain.Result.create_Ok(_default_T);
    }
    static Rtd(rtd\$_T) {
      return class {
        static get Default() {
          return Result.Default(rtd\$_T.Default);
        }
      };
    }
  }
  return \$module;
})(); // end of module KanbanDomain
let KanbanMultiCollaboration = (function() {
  let \$module = {};

  \$module.__default = class __default {
    constructor () {
      this._tname = "KanbanMultiCollaboration._default";
    }
    _parentTraits() {
      return [];
    }
    static Version(s) {
      return new BigNumber(((s).dtor_appliedLog).length);
    };
    static InitServer() {
      return KanbanMultiCollaboration.ServerState.create_ServerState(KanbanDomain.__default.Init(), _dafny.Seq.of(), _dafny.Seq.of());
    };
    static ChooseCandidate(m, cs) {
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((cs).length)).isEqualTo(_dafny.ZERO)) {
          return KanbanDomain.Result.create_Err(KanbanDomain.__default.RejectErr());
        } else {
          let _source0 = KanbanDomain.__default.TryStep(m, (cs)[_dafny.ZERO]);
          {
            if (_source0.is_Ok) {
              let _0_m2 = (_source0).value;
              return KanbanDomain.Result.create_Ok(_dafny.Tuple.of(_0_m2, (cs)[_dafny.ZERO]));
            }
          }
          {
            let _in0 = m;
            let _in1 = (cs).slice(_dafny.ONE);
            m = _in0;
            cs = _in1;
            continue TAIL_CALL_START;
          }
        }
      }
    };
    static Dispatch(s, baseVersion, orig) {
      let _0_suffix = ((s).dtor_appliedLog).slice(baseVersion);
      let _1_rebased = KanbanDomain.__default.RebaseThroughSuffix(_0_suffix, orig);
      let _2_cs = KanbanDomain.__default.Candidates((s).dtor_present, _1_rebased);
      let _source0 = KanbanMultiCollaboration.__default.ChooseCandidate((s).dtor_present, _2_cs);
      {
        if (_source0.is_Ok) {
          let _3_pair = (_source0).value;
          let _4_m2 = (_3_pair)[0];
          let _5_chosen = (_3_pair)[1];
          let _6_noChange = _dafny.areEqual(_4_m2, (s).dtor_present);
          let _7_newApplied = _dafny.Seq.Concat((s).dtor_appliedLog, _dafny.Seq.of(_5_chosen));
          let _8_rec = KanbanMultiCollaboration.RequestRecord.create_Req(baseVersion, orig, _1_rebased, _5_chosen, KanbanMultiCollaboration.RequestOutcome.create_AuditAccepted(_5_chosen, _6_noChange));
          let _9_newAudit = _dafny.Seq.Concat((s).dtor_auditLog, _dafny.Seq.of(_8_rec));
          return _dafny.Tuple.of(KanbanMultiCollaboration.ServerState.create_ServerState(_4_m2, _7_newApplied, _9_newAudit), KanbanMultiCollaboration.Reply.create_Accepted(new BigNumber((_7_newApplied).length), _4_m2, _5_chosen, _6_noChange));
        }
      }
      {
        let _10_rec = KanbanMultiCollaboration.RequestRecord.create_Req(baseVersion, orig, _1_rebased, _1_rebased, KanbanMultiCollaboration.RequestOutcome.create_AuditRejected(KanbanMultiCollaboration.RejectReason.create_DomainInvalid(), _1_rebased));
        let _11_newAudit = _dafny.Seq.Concat((s).dtor_auditLog, _dafny.Seq.of(_10_rec));
        return _dafny.Tuple.of(KanbanMultiCollaboration.ServerState.create_ServerState((s).dtor_present, (s).dtor_appliedLog, _11_newAudit), KanbanMultiCollaboration.Reply.create_Rejected(KanbanMultiCollaboration.RejectReason.create_DomainInvalid(), _1_rebased));
      }
    };
    static InitClient(version, model) {
      return KanbanMultiCollaboration.ClientState.create_ClientState(version, model, _dafny.Seq.of());
    };
    static InitClientFromServer(server) {
      return KanbanMultiCollaboration.ClientState.create_ClientState(KanbanMultiCollaboration.__default.Version(server), (server).dtor_present, _dafny.Seq.of());
    };
    static Sync(server) {
      return KanbanMultiCollaboration.ClientState.create_ClientState(KanbanMultiCollaboration.__default.Version(server), (server).dtor_present, _dafny.Seq.of());
    };
    static ClientLocalDispatch(client, action) {
      let _0_result = KanbanDomain.__default.TryStep((client).dtor_present, action);
      let _source0 = _0_result;
      {
        if (_source0.is_Ok) {
          let _1_newModel = (_source0).value;
          return KanbanMultiCollaboration.ClientState.create_ClientState((client).dtor_baseVersion, _1_newModel, _dafny.Seq.Concat((client).dtor_pending, _dafny.Seq.of(action)));
        }
      }
      {
        return KanbanMultiCollaboration.ClientState.create_ClientState((client).dtor_baseVersion, (client).dtor_present, _dafny.Seq.Concat((client).dtor_pending, _dafny.Seq.of(action)));
      }
    };
    static ReapplyPending(model, pending) {
      TAIL_CALL_START: while (true) {
        if ((new BigNumber((pending).length)).isEqualTo(_dafny.ZERO)) {
          return model;
        } else {
          let _0_result = KanbanDomain.__default.TryStep(model, (pending)[_dafny.ZERO]);
          let _1_newModel = function () {
            let _source0 = _0_result;
            {
              if (_source0.is_Ok) {
                let _2_m = (_source0).value;
                return _2_m;
              }
            }
            {
              return model;
            }
          }();
          let _in0 = _1_newModel;
          let _in1 = (pending).slice(_dafny.ONE);
          model = _in0;
          pending = _in1;
          continue TAIL_CALL_START;
        }
      }
    };
    static HandleRealtimeUpdate(client, serverVersion, serverModel) {
      if (((client).dtor_baseVersion).isLessThan(serverVersion)) {
        let _0_newPresent = KanbanMultiCollaboration.__default.ReapplyPending(serverModel, (client).dtor_pending);
        return KanbanMultiCollaboration.ClientState.create_ClientState(serverVersion, _0_newPresent, (client).dtor_pending);
      } else {
        return client;
      }
    };
    static ClientAcceptReply(client, newVersion, newPresent) {
      if ((new BigNumber(((client).dtor_pending).length)).isEqualTo(_dafny.ZERO)) {
        return KanbanMultiCollaboration.ClientState.create_ClientState(newVersion, newPresent, _dafny.Seq.of());
      } else {
        let _0_rest = ((client).dtor_pending).slice(_dafny.ONE);
        let _1_reappliedPresent = KanbanMultiCollaboration.__default.ReapplyPending(newPresent, _0_rest);
        return KanbanMultiCollaboration.ClientState.create_ClientState(newVersion, _1_reappliedPresent, _0_rest);
      }
    };
    static ClientRejectReply(client, freshVersion, freshModel) {
      if ((new BigNumber(((client).dtor_pending).length)).isEqualTo(_dafny.ZERO)) {
        return KanbanMultiCollaboration.ClientState.create_ClientState(freshVersion, freshModel, _dafny.Seq.of());
      } else {
        let _0_rest = ((client).dtor_pending).slice(_dafny.ONE);
        let _1_reappliedPresent = KanbanMultiCollaboration.__default.ReapplyPending(freshModel, _0_rest);
        return KanbanMultiCollaboration.ClientState.create_ClientState(freshVersion, _1_reappliedPresent, _0_rest);
      }
    };
    static PendingCount(client) {
      return new BigNumber(((client).dtor_pending).length);
    };
    static ClientModel(client) {
      return (client).dtor_present;
    };
    static ClientVersion(client) {
      return (client).dtor_baseVersion;
    };
  };

  \$module.RejectReason = class RejectReason {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_DomainInvalid() {
      let \$dt = new RejectReason(0);
      return \$dt;
    }
    get is_DomainInvalid() { return this.\$tag === 0; }
    static get AllSingletonConstructors() {
      return this.AllSingletonConstructors_();
    }
    static *AllSingletonConstructors_() {
      yield RejectReason.create_DomainInvalid();
    }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.RejectReason.DomainInvalid";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.RejectReason.create_DomainInvalid();
    }
    static Rtd() {
      return class {
        static get Default() {
          return RejectReason.Default();
        }
      };
    }
  }

  \$module.Reply = class Reply {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Accepted(newVersion, newPresent, applied, noChange) {
      let \$dt = new Reply(0);
      \$dt.newVersion = newVersion;
      \$dt.newPresent = newPresent;
      \$dt.applied = applied;
      \$dt.noChange = noChange;
      return \$dt;
    }
    static create_Rejected(reason, rebased) {
      let \$dt = new Reply(1);
      \$dt.reason = reason;
      \$dt.rebased = rebased;
      return \$dt;
    }
    get is_Accepted() { return this.\$tag === 0; }
    get is_Rejected() { return this.\$tag === 1; }
    get dtor_newVersion() { return this.newVersion; }
    get dtor_newPresent() { return this.newPresent; }
    get dtor_applied() { return this.applied; }
    get dtor_noChange() { return this.noChange; }
    get dtor_reason() { return this.reason; }
    get dtor_rebased() { return this.rebased; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.Reply.Accepted" + "(" + _dafny.toString(this.newVersion) + ", " + _dafny.toString(this.newPresent) + ", " + _dafny.toString(this.applied) + ", " + _dafny.toString(this.noChange) + ")";
      } else if (this.\$tag === 1) {
        return "KanbanMultiCollaboration.Reply.Rejected" + "(" + _dafny.toString(this.reason) + ", " + _dafny.toString(this.rebased) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.newVersion, other.newVersion) && _dafny.areEqual(this.newPresent, other.newPresent) && _dafny.areEqual(this.applied, other.applied) && this.noChange === other.noChange;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.reason, other.reason) && _dafny.areEqual(this.rebased, other.rebased);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.Reply.create_Accepted(_dafny.ZERO, KanbanDomain.Model.Default(), KanbanDomain.Action.Default(), false);
    }
    static Rtd() {
      return class {
        static get Default() {
          return Reply.Default();
        }
      };
    }
  }

  \$module.RequestOutcome = class RequestOutcome {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_AuditAccepted(applied, noChange) {
      let \$dt = new RequestOutcome(0);
      \$dt.applied = applied;
      \$dt.noChange = noChange;
      return \$dt;
    }
    static create_AuditRejected(reason, rebased) {
      let \$dt = new RequestOutcome(1);
      \$dt.reason = reason;
      \$dt.rebased = rebased;
      return \$dt;
    }
    get is_AuditAccepted() { return this.\$tag === 0; }
    get is_AuditRejected() { return this.\$tag === 1; }
    get dtor_applied() { return this.applied; }
    get dtor_noChange() { return this.noChange; }
    get dtor_reason() { return this.reason; }
    get dtor_rebased() { return this.rebased; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.RequestOutcome.AuditAccepted" + "(" + _dafny.toString(this.applied) + ", " + _dafny.toString(this.noChange) + ")";
      } else if (this.\$tag === 1) {
        return "KanbanMultiCollaboration.RequestOutcome.AuditRejected" + "(" + _dafny.toString(this.reason) + ", " + _dafny.toString(this.rebased) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.applied, other.applied) && this.noChange === other.noChange;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.reason, other.reason) && _dafny.areEqual(this.rebased, other.rebased);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.RequestOutcome.create_AuditAccepted(KanbanDomain.Action.Default(), false);
    }
    static Rtd() {
      return class {
        static get Default() {
          return RequestOutcome.Default();
        }
      };
    }
  }

  \$module.RequestRecord = class RequestRecord {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Req(baseVersion, orig, rebased, chosen, outcome) {
      let \$dt = new RequestRecord(0);
      \$dt.baseVersion = baseVersion;
      \$dt.orig = orig;
      \$dt.rebased = rebased;
      \$dt.chosen = chosen;
      \$dt.outcome = outcome;
      return \$dt;
    }
    get is_Req() { return this.\$tag === 0; }
    get dtor_baseVersion() { return this.baseVersion; }
    get dtor_orig() { return this.orig; }
    get dtor_rebased() { return this.rebased; }
    get dtor_chosen() { return this.chosen; }
    get dtor_outcome() { return this.outcome; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.RequestRecord.Req" + "(" + _dafny.toString(this.baseVersion) + ", " + _dafny.toString(this.orig) + ", " + _dafny.toString(this.rebased) + ", " + _dafny.toString(this.chosen) + ", " + _dafny.toString(this.outcome) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.baseVersion, other.baseVersion) && _dafny.areEqual(this.orig, other.orig) && _dafny.areEqual(this.rebased, other.rebased) && _dafny.areEqual(this.chosen, other.chosen) && _dafny.areEqual(this.outcome, other.outcome);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.RequestRecord.create_Req(_dafny.ZERO, KanbanDomain.Action.Default(), KanbanDomain.Action.Default(), KanbanDomain.Action.Default(), KanbanMultiCollaboration.RequestOutcome.Default());
    }
    static Rtd() {
      return class {
        static get Default() {
          return RequestRecord.Default();
        }
      };
    }
  }

  \$module.ServerState = class ServerState {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_ServerState(present, appliedLog, auditLog) {
      let \$dt = new ServerState(0);
      \$dt.present = present;
      \$dt.appliedLog = appliedLog;
      \$dt.auditLog = auditLog;
      return \$dt;
    }
    get is_ServerState() { return this.\$tag === 0; }
    get dtor_present() { return this.present; }
    get dtor_appliedLog() { return this.appliedLog; }
    get dtor_auditLog() { return this.auditLog; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.ServerState.ServerState" + "(" + _dafny.toString(this.present) + ", " + _dafny.toString(this.appliedLog) + ", " + _dafny.toString(this.auditLog) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.present, other.present) && _dafny.areEqual(this.appliedLog, other.appliedLog) && _dafny.areEqual(this.auditLog, other.auditLog);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.ServerState.create_ServerState(KanbanDomain.Model.Default(), _dafny.Seq.of(), _dafny.Seq.of());
    }
    static Rtd() {
      return class {
        static get Default() {
          return ServerState.Default();
        }
      };
    }
  }

  \$module.ClientState = class ClientState {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_ClientState(baseVersion, present, pending) {
      let \$dt = new ClientState(0);
      \$dt.baseVersion = baseVersion;
      \$dt.present = present;
      \$dt.pending = pending;
      return \$dt;
    }
    get is_ClientState() { return this.\$tag === 0; }
    get dtor_baseVersion() { return this.baseVersion; }
    get dtor_present() { return this.present; }
    get dtor_pending() { return this.pending; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanMultiCollaboration.ClientState.ClientState" + "(" + _dafny.toString(this.baseVersion) + ", " + _dafny.toString(this.present) + ", " + _dafny.toString(this.pending) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.baseVersion, other.baseVersion) && _dafny.areEqual(this.present, other.present) && _dafny.areEqual(this.pending, other.pending);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanMultiCollaboration.ClientState.create_ClientState(_dafny.ZERO, KanbanDomain.Model.Default(), _dafny.Seq.of());
    }
    static Rtd() {
      return class {
        static get Default() {
          return ClientState.Default();
        }
      };
    }
  }
  return \$module;
})(); // end of module KanbanMultiCollaboration
let KanbanEffectStateMachine = (function() {
  let \$module = {};

  \$module.__default = class __default {
    constructor () {
      this._tname = "KanbanEffectStateMachine._default";
    }
    _parentTraits() {
      return [];
    }
    static PendingCount(es) {
      return KanbanMultiCollaboration.__default.PendingCount((es).dtor_client);
    };
    static HasPending(es) {
      return (_dafny.ZERO).isLessThan(KanbanEffectStateMachine.__default.PendingCount(es));
    };
    static IsOnline(es) {
      return _dafny.areEqual((es).dtor_network, KanbanEffectStateMachine.NetworkStatus.create_Online());
    };
    static IsIdle(es) {
      return _dafny.areEqual((es).dtor_mode, KanbanEffectStateMachine.EffectMode.create_Idle());
    };
    static CanStartDispatch(es) {
      return ((KanbanEffectStateMachine.__default.IsOnline(es)) && (KanbanEffectStateMachine.__default.IsIdle(es))) && (KanbanEffectStateMachine.__default.HasPending(es));
    };
    static FirstPendingAction(es) {
      let _0_pending = ((es).dtor_client).dtor_pending;
      return (_0_pending)[_dafny.ZERO];
    };
    static Step(es, event) {
      let _source0 = event;
      {
        if (_source0.is_UserAction) {
          let _0_action = (_source0).action;
          let _1_newClient = KanbanMultiCollaboration.__default.ClientLocalDispatch((es).dtor_client, _0_action);
          let _2_newState = function (_pat_let0_0) {
            return function (_3_dt__update__tmp_h0) {
              return function (_pat_let1_0) {
                return function (_4_dt__update_hclient_h0) {
                  return KanbanEffectStateMachine.EffectState.create_EffectState((_3_dt__update__tmp_h0).dtor_network, (_3_dt__update__tmp_h0).dtor_mode, _4_dt__update_hclient_h0, (_3_dt__update__tmp_h0).dtor_serverVersion);
                }(_pat_let1_0);
              }(_1_newClient);
            }(_pat_let0_0);
          }(es);
          if (KanbanEffectStateMachine.__default.CanStartDispatch(_2_newState)) {
            return _dafny.Tuple.of(function (_pat_let2_0) {
  return function (_5_dt__update__tmp_h1) {
    return function (_pat_let3_0) {
      return function (_6_dt__update_hmode_h0) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_5_dt__update__tmp_h1).dtor_network, _6_dt__update_hmode_h0, (_5_dt__update__tmp_h1).dtor_client, (_5_dt__update__tmp_h1).dtor_serverVersion);
      }(_pat_let3_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let2_0);
}(_2_newState), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((_2_newState).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(_2_newState)));
          } else {
            return _dafny.Tuple.of(_2_newState, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (_source0.is_DispatchAccepted) {
          let _7_newVersion = (_source0).newVersion;
          let _8_newModel = (_source0).newModel;
          if (((es).dtor_mode).is_Dispatching) {
            let _9_newClient = KanbanMultiCollaboration.__default.ClientAcceptReply((es).dtor_client, _7_newVersion, _8_newModel);
            let _10_newState = KanbanEffectStateMachine.EffectState.create_EffectState((es).dtor_network, KanbanEffectStateMachine.EffectMode.create_Idle(), _9_newClient, _7_newVersion);
            if (KanbanEffectStateMachine.__default.CanStartDispatch(_10_newState)) {
              return _dafny.Tuple.of(function (_pat_let4_0) {
  return function (_11_dt__update__tmp_h2) {
    return function (_pat_let5_0) {
      return function (_12_dt__update_hmode_h1) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_11_dt__update__tmp_h2).dtor_network, _12_dt__update_hmode_h1, (_11_dt__update__tmp_h2).dtor_client, (_11_dt__update__tmp_h2).dtor_serverVersion);
      }(_pat_let5_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let4_0);
}(_10_newState), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((_10_newState).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(_10_newState)));
            } else {
              return _dafny.Tuple.of(_10_newState, KanbanEffectStateMachine.Command.create_NoOp());
            }
          } else {
            return _dafny.Tuple.of(es, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (_source0.is_DispatchConflict) {
          let _13_freshVersion = (_source0).freshVersion;
          let _14_freshModel = (_source0).freshModel;
          if (((es).dtor_mode).is_Dispatching) {
            let _15_retries = ((es).dtor_mode).dtor_retries;
            if ((KanbanEffectStateMachine.__default.MaxRetries).isLessThanOrEqualTo(_15_retries)) {
              return _dafny.Tuple.of(function (_pat_let6_0) {
  return function (_16_dt__update__tmp_h3) {
    return function (_pat_let7_0) {
      return function (_17_dt__update_hmode_h2) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_16_dt__update__tmp_h3).dtor_network, _17_dt__update_hmode_h2, (_16_dt__update__tmp_h3).dtor_client, (_16_dt__update__tmp_h3).dtor_serverVersion);
      }(_pat_let7_0);
    }(KanbanEffectStateMachine.EffectMode.create_Idle());
  }(_pat_let6_0);
}(es), KanbanEffectStateMachine.Command.create_NoOp());
            } else {
              let _18_newClient = KanbanMultiCollaboration.__default.HandleRealtimeUpdate((es).dtor_client, _13_freshVersion, _14_freshModel);
              let _19_newState = KanbanEffectStateMachine.EffectState.create_EffectState((es).dtor_network, KanbanEffectStateMachine.EffectMode.create_Dispatching((_15_retries).plus(_dafny.ONE)), _18_newClient, _13_freshVersion);
              if (KanbanEffectStateMachine.__default.HasPending(_19_newState)) {
                return _dafny.Tuple.of(_19_newState, KanbanEffectStateMachine.Command.create_SendDispatch(_13_freshVersion, KanbanEffectStateMachine.__default.FirstPendingAction(_19_newState)));
              } else {
                return _dafny.Tuple.of(function (_pat_let8_0) {
  return function (_20_dt__update__tmp_h4) {
    return function (_pat_let9_0) {
      return function (_21_dt__update_hmode_h3) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_20_dt__update__tmp_h4).dtor_network, _21_dt__update_hmode_h3, (_20_dt__update__tmp_h4).dtor_client, (_20_dt__update__tmp_h4).dtor_serverVersion);
      }(_pat_let9_0);
    }(KanbanEffectStateMachine.EffectMode.create_Idle());
  }(_pat_let8_0);
}(_19_newState), KanbanEffectStateMachine.Command.create_NoOp());
              }
            }
          } else {
            return _dafny.Tuple.of(es, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (_source0.is_DispatchRejected) {
          let _22_freshVersion = (_source0).freshVersion;
          let _23_freshModel = (_source0).freshModel;
          if (((es).dtor_mode).is_Dispatching) {
            let _24_newClient = KanbanMultiCollaboration.__default.ClientRejectReply((es).dtor_client, _22_freshVersion, _23_freshModel);
            let _25_newState = KanbanEffectStateMachine.EffectState.create_EffectState((es).dtor_network, KanbanEffectStateMachine.EffectMode.create_Idle(), _24_newClient, _22_freshVersion);
            if (KanbanEffectStateMachine.__default.CanStartDispatch(_25_newState)) {
              return _dafny.Tuple.of(function (_pat_let10_0) {
  return function (_26_dt__update__tmp_h5) {
    return function (_pat_let11_0) {
      return function (_27_dt__update_hmode_h4) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_26_dt__update__tmp_h5).dtor_network, _27_dt__update_hmode_h4, (_26_dt__update__tmp_h5).dtor_client, (_26_dt__update__tmp_h5).dtor_serverVersion);
      }(_pat_let11_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let10_0);
}(_25_newState), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((_25_newState).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(_25_newState)));
            } else {
              return _dafny.Tuple.of(_25_newState, KanbanEffectStateMachine.Command.create_NoOp());
            }
          } else {
            return _dafny.Tuple.of(es, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (_source0.is_NetworkError) {
          return _dafny.Tuple.of(function (_pat_let12_0) {
  return function (_28_dt__update__tmp_h6) {
    return function (_pat_let13_0) {
      return function (_29_dt__update_hmode_h5) {
        return function (_pat_let14_0) {
          return function (_30_dt__update_hnetwork_h0) {
            return KanbanEffectStateMachine.EffectState.create_EffectState(_30_dt__update_hnetwork_h0, _29_dt__update_hmode_h5, (_28_dt__update__tmp_h6).dtor_client, (_28_dt__update__tmp_h6).dtor_serverVersion);
          }(_pat_let14_0);
        }(KanbanEffectStateMachine.NetworkStatus.create_Offline());
      }(_pat_let13_0);
    }(KanbanEffectStateMachine.EffectMode.create_Idle());
  }(_pat_let12_0);
}(es), KanbanEffectStateMachine.Command.create_NoOp());
        }
      }
      {
        if (_source0.is_NetworkRestored) {
          let _31_newState = function (_pat_let15_0) {
            return function (_32_dt__update__tmp_h7) {
              return function (_pat_let16_0) {
                return function (_33_dt__update_hnetwork_h1) {
                  return KanbanEffectStateMachine.EffectState.create_EffectState(_33_dt__update_hnetwork_h1, (_32_dt__update__tmp_h7).dtor_mode, (_32_dt__update__tmp_h7).dtor_client, (_32_dt__update__tmp_h7).dtor_serverVersion);
                }(_pat_let16_0);
              }(KanbanEffectStateMachine.NetworkStatus.create_Online());
            }(_pat_let15_0);
          }(es);
          if (KanbanEffectStateMachine.__default.CanStartDispatch(_31_newState)) {
            return _dafny.Tuple.of(function (_pat_let17_0) {
  return function (_34_dt__update__tmp_h8) {
    return function (_pat_let18_0) {
      return function (_35_dt__update_hmode_h6) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_34_dt__update__tmp_h8).dtor_network, _35_dt__update_hmode_h6, (_34_dt__update__tmp_h8).dtor_client, (_34_dt__update__tmp_h8).dtor_serverVersion);
      }(_pat_let18_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let17_0);
}(_31_newState), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((_31_newState).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(_31_newState)));
          } else {
            return _dafny.Tuple.of(_31_newState, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (_source0.is_ManualGoOffline) {
          return _dafny.Tuple.of(function (_pat_let19_0) {
  return function (_36_dt__update__tmp_h9) {
    return function (_pat_let20_0) {
      return function (_37_dt__update_hmode_h7) {
        return function (_pat_let21_0) {
          return function (_38_dt__update_hnetwork_h2) {
            return KanbanEffectStateMachine.EffectState.create_EffectState(_38_dt__update_hnetwork_h2, _37_dt__update_hmode_h7, (_36_dt__update__tmp_h9).dtor_client, (_36_dt__update__tmp_h9).dtor_serverVersion);
          }(_pat_let21_0);
        }(KanbanEffectStateMachine.NetworkStatus.create_Offline());
      }(_pat_let20_0);
    }(KanbanEffectStateMachine.EffectMode.create_Idle());
  }(_pat_let19_0);
}(es), KanbanEffectStateMachine.Command.create_NoOp());
        }
      }
      {
        if (_source0.is_ManualGoOnline) {
          let _39_newState = function (_pat_let22_0) {
            return function (_40_dt__update__tmp_h10) {
              return function (_pat_let23_0) {
                return function (_41_dt__update_hnetwork_h3) {
                  return KanbanEffectStateMachine.EffectState.create_EffectState(_41_dt__update_hnetwork_h3, (_40_dt__update__tmp_h10).dtor_mode, (_40_dt__update__tmp_h10).dtor_client, (_40_dt__update__tmp_h10).dtor_serverVersion);
                }(_pat_let23_0);
              }(KanbanEffectStateMachine.NetworkStatus.create_Online());
            }(_pat_let22_0);
          }(es);
          if (KanbanEffectStateMachine.__default.CanStartDispatch(_39_newState)) {
            return _dafny.Tuple.of(function (_pat_let24_0) {
  return function (_42_dt__update__tmp_h11) {
    return function (_pat_let25_0) {
      return function (_43_dt__update_hmode_h8) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_42_dt__update__tmp_h11).dtor_network, _43_dt__update_hmode_h8, (_42_dt__update__tmp_h11).dtor_client, (_42_dt__update__tmp_h11).dtor_serverVersion);
      }(_pat_let25_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let24_0);
}(_39_newState), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((_39_newState).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(_39_newState)));
          } else {
            return _dafny.Tuple.of(_39_newState, KanbanEffectStateMachine.Command.create_NoOp());
          }
        }
      }
      {
        if (KanbanEffectStateMachine.__default.CanStartDispatch(es)) {
          return _dafny.Tuple.of(function (_pat_let26_0) {
  return function (_44_dt__update__tmp_h12) {
    return function (_pat_let27_0) {
      return function (_45_dt__update_hmode_h9) {
        return KanbanEffectStateMachine.EffectState.create_EffectState((_44_dt__update__tmp_h12).dtor_network, _45_dt__update_hmode_h9, (_44_dt__update__tmp_h12).dtor_client, (_44_dt__update__tmp_h12).dtor_serverVersion);
      }(_pat_let27_0);
    }(KanbanEffectStateMachine.EffectMode.create_Dispatching(_dafny.ZERO));
  }(_pat_let26_0);
}(es), KanbanEffectStateMachine.Command.create_SendDispatch(KanbanMultiCollaboration.__default.ClientVersion((es).dtor_client), KanbanEffectStateMachine.__default.FirstPendingAction(es)));
        } else {
          return _dafny.Tuple.of(es, KanbanEffectStateMachine.Command.create_NoOp());
        }
      }
    };
    static ModeConsistent(es) {
      return !(((es).dtor_mode).is_Dispatching) || (KanbanEffectStateMachine.__default.HasPending(es));
    };
    static RetriesBounded(es) {
      return !(((es).dtor_mode).is_Dispatching) || ((((es).dtor_mode).dtor_retries).isLessThanOrEqualTo(KanbanEffectStateMachine.__default.MaxRetries));
    };
    static Inv(es) {
      return (KanbanEffectStateMachine.__default.ModeConsistent(es)) && (KanbanEffectStateMachine.__default.RetriesBounded(es));
    };
    static Pending(es) {
      return ((es).dtor_client).dtor_pending;
    };
    static ProgressMeasure(es) {
      return _dafny.Tuple.of(((es).dtor_mode).is_Dispatching, (((((es).dtor_mode).is_Dispatching) && ((((es).dtor_mode).dtor_retries).isLessThanOrEqualTo(KanbanEffectStateMachine.__default.MaxRetries))) ? ((KanbanEffectStateMachine.__default.MaxRetries).minus(((es).dtor_mode).dtor_retries)) : (_dafny.ZERO)), KanbanEffectStateMachine.__default.PendingCount(es));
    };
    static ProgressLt(m1, m2) {
      return ((((m1)[2]).isLessThan((m2)[2])) || (((((m1)[2]).isEqualTo((m2)[2])) && (!((m1)[0]))) && ((m2)[0]))) || (((((m1)[2]).isEqualTo((m2)[2])) && (((m1)[0]) === ((m2)[0]))) && (((m1)[1]).isLessThan((m2)[1])));
    };
    static Init(version, model) {
      return KanbanEffectStateMachine.EffectState.create_EffectState(KanbanEffectStateMachine.NetworkStatus.create_Online(), KanbanEffectStateMachine.EffectMode.create_Idle(), KanbanMultiCollaboration.__default.InitClient(version, model), version);
    };
    static get MaxRetries() {
      return new BigNumber(5);
    };
  };

  \$module.NetworkStatus = class NetworkStatus {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Online() {
      let \$dt = new NetworkStatus(0);
      return \$dt;
    }
    static create_Offline() {
      let \$dt = new NetworkStatus(1);
      return \$dt;
    }
    get is_Online() { return this.\$tag === 0; }
    get is_Offline() { return this.\$tag === 1; }
    static get AllSingletonConstructors() {
      return this.AllSingletonConstructors_();
    }
    static *AllSingletonConstructors_() {
      yield NetworkStatus.create_Online();
      yield NetworkStatus.create_Offline();
    }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanEffectStateMachine.NetworkStatus.Online";
      } else if (this.\$tag === 1) {
        return "KanbanEffectStateMachine.NetworkStatus.Offline";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1;
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanEffectStateMachine.NetworkStatus.create_Online();
    }
    static Rtd() {
      return class {
        static get Default() {
          return NetworkStatus.Default();
        }
      };
    }
  }

  \$module.EffectMode = class EffectMode {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_Idle() {
      let \$dt = new EffectMode(0);
      return \$dt;
    }
    static create_Dispatching(retries) {
      let \$dt = new EffectMode(1);
      \$dt.retries = retries;
      return \$dt;
    }
    get is_Idle() { return this.\$tag === 0; }
    get is_Dispatching() { return this.\$tag === 1; }
    get dtor_retries() { return this.retries; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanEffectStateMachine.EffectMode.Idle";
      } else if (this.\$tag === 1) {
        return "KanbanEffectStateMachine.EffectMode.Dispatching" + "(" + _dafny.toString(this.retries) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.retries, other.retries);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanEffectStateMachine.EffectMode.create_Idle();
    }
    static Rtd() {
      return class {
        static get Default() {
          return EffectMode.Default();
        }
      };
    }
  }

  \$module.EffectState = class EffectState {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_EffectState(network, mode, client, serverVersion) {
      let \$dt = new EffectState(0);
      \$dt.network = network;
      \$dt.mode = mode;
      \$dt.client = client;
      \$dt.serverVersion = serverVersion;
      return \$dt;
    }
    get is_EffectState() { return this.\$tag === 0; }
    get dtor_network() { return this.network; }
    get dtor_mode() { return this.mode; }
    get dtor_client() { return this.client; }
    get dtor_serverVersion() { return this.serverVersion; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanEffectStateMachine.EffectState.EffectState" + "(" + _dafny.toString(this.network) + ", " + _dafny.toString(this.mode) + ", " + _dafny.toString(this.client) + ", " + _dafny.toString(this.serverVersion) + ")";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.network, other.network) && _dafny.areEqual(this.mode, other.mode) && _dafny.areEqual(this.client, other.client) && _dafny.areEqual(this.serverVersion, other.serverVersion);
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanEffectStateMachine.EffectState.create_EffectState(KanbanEffectStateMachine.NetworkStatus.Default(), KanbanEffectStateMachine.EffectMode.Default(), KanbanMultiCollaboration.ClientState.Default(), _dafny.ZERO);
    }
    static Rtd() {
      return class {
        static get Default() {
          return EffectState.Default();
        }
      };
    }
  }

  \$module.Event = class Event {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_UserAction(action) {
      let \$dt = new Event(0);
      \$dt.action = action;
      return \$dt;
    }
    static create_DispatchAccepted(newVersion, newModel) {
      let \$dt = new Event(1);
      \$dt.newVersion = newVersion;
      \$dt.newModel = newModel;
      return \$dt;
    }
    static create_DispatchConflict(freshVersion, freshModel) {
      let \$dt = new Event(2);
      \$dt.freshVersion = freshVersion;
      \$dt.freshModel = freshModel;
      return \$dt;
    }
    static create_DispatchRejected(freshVersion, freshModel) {
      let \$dt = new Event(3);
      \$dt.freshVersion = freshVersion;
      \$dt.freshModel = freshModel;
      return \$dt;
    }
    static create_NetworkError() {
      let \$dt = new Event(4);
      return \$dt;
    }
    static create_NetworkRestored() {
      let \$dt = new Event(5);
      return \$dt;
    }
    static create_ManualGoOffline() {
      let \$dt = new Event(6);
      return \$dt;
    }
    static create_ManualGoOnline() {
      let \$dt = new Event(7);
      return \$dt;
    }
    static create_Tick() {
      let \$dt = new Event(8);
      return \$dt;
    }
    get is_UserAction() { return this.\$tag === 0; }
    get is_DispatchAccepted() { return this.\$tag === 1; }
    get is_DispatchConflict() { return this.\$tag === 2; }
    get is_DispatchRejected() { return this.\$tag === 3; }
    get is_NetworkError() { return this.\$tag === 4; }
    get is_NetworkRestored() { return this.\$tag === 5; }
    get is_ManualGoOffline() { return this.\$tag === 6; }
    get is_ManualGoOnline() { return this.\$tag === 7; }
    get is_Tick() { return this.\$tag === 8; }
    get dtor_action() { return this.action; }
    get dtor_newVersion() { return this.newVersion; }
    get dtor_newModel() { return this.newModel; }
    get dtor_freshVersion() { return this.freshVersion; }
    get dtor_freshModel() { return this.freshModel; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanEffectStateMachine.Event.UserAction" + "(" + _dafny.toString(this.action) + ")";
      } else if (this.\$tag === 1) {
        return "KanbanEffectStateMachine.Event.DispatchAccepted" + "(" + _dafny.toString(this.newVersion) + ", " + _dafny.toString(this.newModel) + ")";
      } else if (this.\$tag === 2) {
        return "KanbanEffectStateMachine.Event.DispatchConflict" + "(" + _dafny.toString(this.freshVersion) + ", " + _dafny.toString(this.freshModel) + ")";
      } else if (this.\$tag === 3) {
        return "KanbanEffectStateMachine.Event.DispatchRejected" + "(" + _dafny.toString(this.freshVersion) + ", " + _dafny.toString(this.freshModel) + ")";
      } else if (this.\$tag === 4) {
        return "KanbanEffectStateMachine.Event.NetworkError";
      } else if (this.\$tag === 5) {
        return "KanbanEffectStateMachine.Event.NetworkRestored";
      } else if (this.\$tag === 6) {
        return "KanbanEffectStateMachine.Event.ManualGoOffline";
      } else if (this.\$tag === 7) {
        return "KanbanEffectStateMachine.Event.ManualGoOnline";
      } else if (this.\$tag === 8) {
        return "KanbanEffectStateMachine.Event.Tick";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0 && _dafny.areEqual(this.action, other.action);
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.newVersion, other.newVersion) && _dafny.areEqual(this.newModel, other.newModel);
      } else if (this.\$tag === 2) {
        return other.\$tag === 2 && _dafny.areEqual(this.freshVersion, other.freshVersion) && _dafny.areEqual(this.freshModel, other.freshModel);
      } else if (this.\$tag === 3) {
        return other.\$tag === 3 && _dafny.areEqual(this.freshVersion, other.freshVersion) && _dafny.areEqual(this.freshModel, other.freshModel);
      } else if (this.\$tag === 4) {
        return other.\$tag === 4;
      } else if (this.\$tag === 5) {
        return other.\$tag === 5;
      } else if (this.\$tag === 6) {
        return other.\$tag === 6;
      } else if (this.\$tag === 7) {
        return other.\$tag === 7;
      } else if (this.\$tag === 8) {
        return other.\$tag === 8;
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanEffectStateMachine.Event.create_UserAction(KanbanDomain.Action.Default());
    }
    static Rtd() {
      return class {
        static get Default() {
          return Event.Default();
        }
      };
    }
  }

  \$module.Command = class Command {
    constructor(tag) {
      this.\$tag = tag;
    }
    static create_NoOp() {
      let \$dt = new Command(0);
      return \$dt;
    }
    static create_SendDispatch(baseVersion, action) {
      let \$dt = new Command(1);
      \$dt.baseVersion = baseVersion;
      \$dt.action = action;
      return \$dt;
    }
    static create_FetchFreshState() {
      let \$dt = new Command(2);
      return \$dt;
    }
    get is_NoOp() { return this.\$tag === 0; }
    get is_SendDispatch() { return this.\$tag === 1; }
    get is_FetchFreshState() { return this.\$tag === 2; }
    get dtor_baseVersion() { return this.baseVersion; }
    get dtor_action() { return this.action; }
    toString() {
      if (this.\$tag === 0) {
        return "KanbanEffectStateMachine.Command.NoOp";
      } else if (this.\$tag === 1) {
        return "KanbanEffectStateMachine.Command.SendDispatch" + "(" + _dafny.toString(this.baseVersion) + ", " + _dafny.toString(this.action) + ")";
      } else if (this.\$tag === 2) {
        return "KanbanEffectStateMachine.Command.FetchFreshState";
      } else  {
        return "<unexpected>";
      }
    }
    equals(other) {
      if (this === other) {
        return true;
      } else if (this.\$tag === 0) {
        return other.\$tag === 0;
      } else if (this.\$tag === 1) {
        return other.\$tag === 1 && _dafny.areEqual(this.baseVersion, other.baseVersion) && _dafny.areEqual(this.action, other.action);
      } else if (this.\$tag === 2) {
        return other.\$tag === 2;
      } else  {
        return false; // unexpected
      }
    }
    static Default() {
      return KanbanEffectStateMachine.Command.create_NoOp();
    }
    static Rtd() {
      return class {
        static get Default() {
          return Command.Default();
        }
      };
    }
  }
  return \$module;
})(); // end of module KanbanEffectStateMachine
let KanbanAppCore = (function() {
  let \$module = {};

  \$module.__default = class __default {
    constructor () {
      this._tname = "KanbanAppCore._default";
    }
    _parentTraits() {
      return [];
    }
    static MakeClientState(baseVersion, present, pending) {
      return KanbanMultiCollaboration.ClientState.create_ClientState(baseVersion, present, pending);
    };
    static Init() {
      return KanbanMultiCollaboration.__default.InitServer();
    };
    static InitServerWithModel(initModel) {
      return KanbanMultiCollaboration.ServerState.create_ServerState(initModel, _dafny.Seq.of(), _dafny.Seq.of());
    };
    static InitClientFromServer(server) {
      return KanbanMultiCollaboration.__default.InitClientFromServer(server);
    };
    static ClientLocalDispatch(client, action) {
      return KanbanMultiCollaboration.__default.ClientLocalDispatch(client, action);
    };
    static HandleRealtimeUpdate(client, serverVersion, serverModel) {
      return KanbanMultiCollaboration.__default.HandleRealtimeUpdate(client, serverVersion, serverModel);
    };
    static ClientAcceptReply(client, newVersion, newPresent) {
      return KanbanMultiCollaboration.__default.ClientAcceptReply(client, newVersion, newPresent);
    };
    static Sync(server) {
      return KanbanMultiCollaboration.__default.Sync(server);
    };
    static ServerVersion(server) {
      return KanbanMultiCollaboration.__default.Version(server);
    };
    static ServerModel(server) {
      return (server).dtor_present;
    };
    static AuditLength(server) {
      return new BigNumber(((server).dtor_auditLog).length);
    };
    static PendingCount(client) {
      return KanbanMultiCollaboration.__default.PendingCount(client);
    };
    static ClientModel(client) {
      return KanbanMultiCollaboration.__default.ClientModel(client);
    };
    static ClientVersion(client) {
      return KanbanMultiCollaboration.__default.ClientVersion(client);
    };
    static IsAccepted(reply) {
      return (reply).is_Accepted;
    };
    static IsRejected(reply) {
      return (reply).is_Rejected;
    };
  };
  return \$module;
})(); // end of module KanbanAppCore
let KanbanEffectAppCore = (function() {
  let \$module = {};

  \$module.__default = class __default {
    constructor () {
      this._tname = "KanbanEffectAppCore._default";
    }
    _parentTraits() {
      return [];
    }
    static EffectInit(version, model) {
      return KanbanEffectStateMachine.__default.Init(version, model);
    };
    static EffectStep(es, event) {
      return KanbanEffectStateMachine.__default.Step(es, event);
    };
    static EffectIsOnline(es) {
      return KanbanEffectStateMachine.__default.IsOnline(es);
    };
    static EffectIsIdle(es) {
      return KanbanEffectStateMachine.__default.IsIdle(es);
    };
    static EffectHasPending(es) {
      return KanbanEffectStateMachine.__default.HasPending(es);
    };
    static EffectPendingCount(es) {
      return KanbanEffectStateMachine.__default.PendingCount(es);
    };
    static EffectGetClient(es) {
      return (es).dtor_client;
    };
    static EffectGetServerVersion(es) {
      return (es).dtor_serverVersion;
    };
    static EffectUserAction(action) {
      return KanbanEffectStateMachine.Event.create_UserAction(action);
    };
    static EffectDispatchAccepted(version, model) {
      return KanbanEffectStateMachine.Event.create_DispatchAccepted(version, model);
    };
    static EffectDispatchConflict(version, model) {
      return KanbanEffectStateMachine.Event.create_DispatchConflict(version, model);
    };
    static EffectDispatchRejected(version, model) {
      return KanbanEffectStateMachine.Event.create_DispatchRejected(version, model);
    };
    static EffectNetworkError() {
      return KanbanEffectStateMachine.Event.create_NetworkError();
    };
    static EffectNetworkRestored() {
      return KanbanEffectStateMachine.Event.create_NetworkRestored();
    };
    static EffectManualGoOffline() {
      return KanbanEffectStateMachine.Event.create_ManualGoOffline();
    };
    static EffectManualGoOnline() {
      return KanbanEffectStateMachine.Event.create_ManualGoOnline();
    };
    static EffectTick() {
      return KanbanEffectStateMachine.Event.create_Tick();
    };
    static EffectIsNoOp(cmd) {
      return (cmd).is_NoOp;
    };
    static EffectIsSendDispatch(cmd) {
      return (cmd).is_SendDispatch;
    };
    static EffectGetBaseVersion(cmd) {
      return (cmd).dtor_baseVersion;
    };
    static EffectGetAction(cmd) {
      return (cmd).dtor_action;
    };
    static MakeClientState(baseVersion, present, pending) {
      return KanbanMultiCollaboration.ClientState.create_ClientState(baseVersion, present, pending);
    };
    static Init() {
      return KanbanMultiCollaboration.__default.InitServer();
    };
    static InitServerWithModel(initModel) {
      return KanbanMultiCollaboration.ServerState.create_ServerState(initModel, _dafny.Seq.of(), _dafny.Seq.of());
    };
    static InitClientFromServer(server) {
      return KanbanMultiCollaboration.__default.InitClientFromServer(server);
    };
    static ClientLocalDispatch(client, action) {
      return KanbanMultiCollaboration.__default.ClientLocalDispatch(client, action);
    };
    static HandleRealtimeUpdate(client, serverVersion, serverModel) {
      return KanbanMultiCollaboration.__default.HandleRealtimeUpdate(client, serverVersion, serverModel);
    };
    static ClientAcceptReply(client, newVersion, newPresent) {
      return KanbanMultiCollaboration.__default.ClientAcceptReply(client, newVersion, newPresent);
    };
    static Sync(server) {
      return KanbanMultiCollaboration.__default.Sync(server);
    };
    static ServerVersion(server) {
      return KanbanMultiCollaboration.__default.Version(server);
    };
    static ServerModel(server) {
      return (server).dtor_present;
    };
    static AuditLength(server) {
      return new BigNumber(((server).dtor_auditLog).length);
    };
    static PendingCount(client) {
      return KanbanMultiCollaboration.__default.PendingCount(client);
    };
    static ClientModel(client) {
      return KanbanMultiCollaboration.__default.ClientModel(client);
    };
    static ClientVersion(client) {
      return KanbanMultiCollaboration.__default.ClientVersion(client);
    };
    static IsAccepted(reply) {
      return (reply).is_Accepted;
    };
    static IsRejected(reply) {
      return (reply).is_Rejected;
    };
  };
  return \$module;
})(); // end of module KanbanEffectAppCore
let _module = (function() {
  let \$module = {};

  return \$module;
})(); // end of module _module

  return { _dafny, KanbanDomain, KanbanMultiCollaboration, KanbanEffectStateMachine, KanbanEffectAppCore };
`);

const { _dafny, KanbanDomain, KanbanMultiCollaboration, KanbanEffectStateMachine, KanbanEffectAppCore } = initDafny(require, exports, module);

// ============================================================================
// Helpers
// ============================================================================

// deno-lint-ignore no-explicit-any
const seqToArray = (seq: any): any[] => {
  const arr = [];
  for (let i = 0; i < seq.length; i++) {
    arr.push(seq[i]);
  }
  return arr;
};

// deno-lint-ignore no-explicit-any
const toNumber = (bn: any): number => {
  if (bn && typeof bn.toNumber === 'function') {
    return bn.toNumber();
  }
  return bn;
};

// deno-lint-ignore no-explicit-any
const dafnyStringToJs = (seq: any): string => {
  if (typeof seq === 'string') return seq;
  if (seq.toVerbatimString) return seq.toVerbatimString(false);
  return Array.from(seq).join('');
};


// ============================================================================
// TypeScript Type Definitions (JSON representation)
// ============================================================================

export interface Card {
  title: string;
}

export interface Model {
  cols: string[];
  lanes: Record<string, number[]>;
  wip: Record<string, number>;
  cards: Record<string, Card>;
  nextId: number;
}

export type Err = 'MissingColumn' | 'MissingCard' | 'WipExceeded' | 'BadAnchor' | 'Rejected';

export type Option<T> =
  | { type: 'None' }
  | { type: 'Some'; value: unknown };

export type Place =
  | { type: 'AtEnd' }
  | { type: 'Before'; anchor: number }
  | { type: 'After'; anchor: number };

export type Action =
  | { type: 'NoOp' }
  | { type: 'AddColumn'; col: string; limit: number }
  | { type: 'SetWip'; col: string; limit: number }
  | { type: 'AddCard'; col: string; title: string }
  | { type: 'MoveCard'; id: number; toCol: string; place: Place }
  | { type: 'EditTitle'; id: number; title: string };

export type Result<T, E> =
  | { type: 'Ok'; value: unknown }
  | { type: 'Err'; error: unknown };

export interface RejectReason {
}

export type Reply =
  | { type: 'Accepted'; newVersion: number; newPresent: Model; applied: Action; noChange: boolean }
  | { type: 'Rejected'; reason: RejectReason; rebased: Action };

export type RequestOutcome =
  | { type: 'AuditAccepted'; applied: Action; noChange: boolean }
  | { type: 'AuditRejected'; reason: RejectReason; rebased: Action };

export interface RequestRecord {
  baseVersion: number;
  orig: Action;
  rebased: Action;
  chosen: Action;
  outcome: RequestOutcome;
}

export interface ServerState {
  present: Model;
  appliedLog: Action[];
  auditLog: RequestRecord[];
}

export interface ClientState {
  baseVersion: number;
  present: Model;
  pending: Action[];
}

export type NetworkStatus = 'Online' | 'Offline';

export type EffectMode =
  | { type: 'Idle' }
  | { type: 'Dispatching'; retries: number };

export interface EffectState {
  network: NetworkStatus;
  mode: EffectMode;
  client: ClientState;
  serverVersion: number;
}

export type Event =
  | { type: 'UserAction'; action: Action }
  | { type: 'DispatchAccepted'; newVersion: number; newModel: Model }
  | { type: 'DispatchConflict'; freshVersion: number; freshModel: Model }
  | { type: 'DispatchRejected'; freshVersion: number; freshModel: Model }
  | { type: 'NetworkError' }
  | { type: 'NetworkRestored' }
  | { type: 'ManualGoOffline' }
  | { type: 'ManualGoOnline' }
  | { type: 'Tick' };

export type Command =
  | { type: 'NoOp' }
  | { type: 'SendDispatch'; baseVersion: number; action: Action }
  | { type: 'FetchFreshState' };

// ============================================================================
// Dafny Runtime Types (actual Dafny runtime object shapes)
// ============================================================================

// Base Dafny runtime types
type DafnyInt = InstanceType<typeof BigNumber>;
interface DafnySeq<T = unknown> {
  readonly length: number;
  readonly [index: number]: T;
  toVerbatimString?(asLiteral: boolean): string;
  map<U>(fn: (x: T) => U): U[];
}
interface DafnySet<T = unknown> { readonly Elements: Iterable<T>; }
interface DafnyMap<K = unknown, V = unknown> {
  readonly Keys: DafnySet<K>;
  get(key: K): V;
  contains(key: K): boolean;
}
type DafnyTuple2<T0, T1> = readonly [T0, T1];

type DafnyCard = DafnySeq;

interface DafnyModel {
  readonly is_Model: true;
  readonly dtor_cols: DafnySeq<DafnySeq>;
  readonly dtor_lanes: DafnyMap<DafnySeq, DafnySeq<DafnyInt>>;
  readonly dtor_wip: DafnyMap<DafnySeq, DafnyInt>;
  readonly dtor_cards: DafnyMap<DafnyInt, DafnyCard>;
  readonly dtor_nextId: DafnyInt;
}

type DafnyErr = { readonly is_MissingColumn: true; readonly is_MissingCard: false; readonly is_WipExceeded: false; readonly is_BadAnchor: false; readonly is_Rejected: false } | { readonly is_MissingColumn: false; readonly is_MissingCard: true; readonly is_WipExceeded: false; readonly is_BadAnchor: false; readonly is_Rejected: false } | { readonly is_MissingColumn: false; readonly is_MissingCard: false; readonly is_WipExceeded: true; readonly is_BadAnchor: false; readonly is_Rejected: false } | { readonly is_MissingColumn: false; readonly is_MissingCard: false; readonly is_WipExceeded: false; readonly is_BadAnchor: true; readonly is_Rejected: false } | { readonly is_MissingColumn: false; readonly is_MissingCard: false; readonly is_WipExceeded: false; readonly is_BadAnchor: false; readonly is_Rejected: true };

type DafnyOption<T> = { readonly is_None: true; readonly is_Some: false } | { readonly is_None: false; readonly is_Some: true; readonly dtor_value: T };

type DafnyPlace = { readonly is_AtEnd: true; readonly is_Before: false; readonly is_After: false } | { readonly is_AtEnd: false; readonly is_Before: true; readonly is_After: false; readonly dtor_anchor: DafnyInt } | { readonly is_AtEnd: false; readonly is_Before: false; readonly is_After: true; readonly dtor_anchor: DafnyInt };

type DafnyAction = { readonly is_NoOp: true; readonly is_AddColumn: false; readonly is_SetWip: false; readonly is_AddCard: false; readonly is_MoveCard: false; readonly is_EditTitle: false } | { readonly is_NoOp: false; readonly is_AddColumn: true; readonly is_SetWip: false; readonly is_AddCard: false; readonly is_MoveCard: false; readonly is_EditTitle: false; readonly dtor_col: DafnySeq; readonly dtor_limit: DafnyInt } | { readonly is_NoOp: false; readonly is_AddColumn: false; readonly is_SetWip: true; readonly is_AddCard: false; readonly is_MoveCard: false; readonly is_EditTitle: false; readonly dtor_col: DafnySeq; readonly dtor_limit: DafnyInt } | { readonly is_NoOp: false; readonly is_AddColumn: false; readonly is_SetWip: false; readonly is_AddCard: true; readonly is_MoveCard: false; readonly is_EditTitle: false; readonly dtor_col: DafnySeq; readonly dtor_title: DafnySeq } | { readonly is_NoOp: false; readonly is_AddColumn: false; readonly is_SetWip: false; readonly is_AddCard: false; readonly is_MoveCard: true; readonly is_EditTitle: false; readonly dtor_id: DafnyInt; readonly dtor_toCol: DafnySeq; readonly dtor_place: DafnyPlace } | { readonly is_NoOp: false; readonly is_AddColumn: false; readonly is_SetWip: false; readonly is_AddCard: false; readonly is_MoveCard: false; readonly is_EditTitle: true; readonly dtor_id: DafnyInt; readonly dtor_title: DafnySeq };

type DafnyResult<T, E> = { readonly is_Ok: true; readonly is_Err: false; readonly dtor_value: T } | { readonly is_Ok: false; readonly is_Err: true; readonly dtor_error: E };

interface DafnyRejectReason {
  readonly is_DomainInvalid: true;
}

type DafnyReply = { readonly is_Accepted: true; readonly is_Rejected: false; readonly dtor_newVersion: DafnyInt; readonly dtor_newPresent: DafnyModel; readonly dtor_applied: DafnyAction; readonly dtor_noChange: boolean } | { readonly is_Accepted: false; readonly is_Rejected: true; readonly dtor_reason: DafnyRejectReason; readonly dtor_rebased: DafnyAction };

type DafnyRequestOutcome = { readonly is_AuditAccepted: true; readonly is_AuditRejected: false; readonly dtor_applied: DafnyAction; readonly dtor_noChange: boolean } | { readonly is_AuditAccepted: false; readonly is_AuditRejected: true; readonly dtor_reason: DafnyRejectReason; readonly dtor_rebased: DafnyAction };

interface DafnyRequestRecord {
  readonly is_Req: true;
  readonly dtor_baseVersion: DafnyInt;
  readonly dtor_orig: DafnyAction;
  readonly dtor_rebased: DafnyAction;
  readonly dtor_chosen: DafnyAction;
  readonly dtor_outcome: DafnyRequestOutcome;
}

interface DafnyServerState {
  readonly is_ServerState: true;
  readonly dtor_present: DafnyModel;
  readonly dtor_appliedLog: DafnySeq<DafnyAction>;
  readonly dtor_auditLog: DafnySeq<DafnyRequestRecord>;
}

interface DafnyClientState {
  readonly is_ClientState: true;
  readonly dtor_baseVersion: DafnyInt;
  readonly dtor_present: DafnyModel;
  readonly dtor_pending: DafnySeq<DafnyAction>;
}

type DafnyNetworkStatus = { readonly is_Online: true; readonly is_Offline: false } | { readonly is_Online: false; readonly is_Offline: true };

type DafnyEffectMode = { readonly is_Idle: true; readonly is_Dispatching: false } | { readonly is_Idle: false; readonly is_Dispatching: true; readonly dtor_retries: DafnyInt };

interface DafnyEffectState {
  readonly is_EffectState: true;
  readonly dtor_network: DafnyNetworkStatus;
  readonly dtor_mode: DafnyEffectMode;
  readonly dtor_client: DafnyClientState;
  readonly dtor_serverVersion: DafnyInt;
}

type DafnyEvent = { readonly is_UserAction: true; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false; readonly dtor_action: DafnyAction } | { readonly is_UserAction: false; readonly is_DispatchAccepted: true; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false; readonly dtor_newVersion: DafnyInt; readonly dtor_newModel: DafnyModel } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: true; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false; readonly dtor_freshVersion: DafnyInt; readonly dtor_freshModel: DafnyModel } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: true; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false; readonly dtor_freshVersion: DafnyInt; readonly dtor_freshModel: DafnyModel } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: true; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: true; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: false } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: true; readonly is_ManualGoOnline: false; readonly is_Tick: false } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: true; readonly is_Tick: false } | { readonly is_UserAction: false; readonly is_DispatchAccepted: false; readonly is_DispatchConflict: false; readonly is_DispatchRejected: false; readonly is_NetworkError: false; readonly is_NetworkRestored: false; readonly is_ManualGoOffline: false; readonly is_ManualGoOnline: false; readonly is_Tick: true };

type DafnyCommand = { readonly is_NoOp: true; readonly is_SendDispatch: false; readonly is_FetchFreshState: false } | { readonly is_NoOp: false; readonly is_SendDispatch: true; readonly is_FetchFreshState: false; readonly dtor_baseVersion: DafnyInt; readonly dtor_action: DafnyAction } | { readonly is_NoOp: false; readonly is_SendDispatch: false; readonly is_FetchFreshState: true };

// ============================================================================
// Datatype Conversions
// ============================================================================

// deno-lint-ignore no-explicit-any
const cardFromJson = (json: any): DafnyCard => {
  return _dafny.Seq.UnicodeFromString(json.title);
};

// deno-lint-ignore no-explicit-any
const cardToJson = (value: any): Card => {
  return { title: dafnyStringToJs(value) };
};

// deno-lint-ignore no-explicit-any
const modelFromJson = (json: any): DafnyModel => {
  let __lanes = _dafny.Map.Empty;
  for (const [k, v] of (Object.entries(json.lanes || {}) as [string, any][])) {
    const key = _dafny.Seq.UnicodeFromString(k);
    const val = _dafny.Seq.of(...(v || []).map((x: any) => new BigNumber(x)));
    __lanes = __lanes.update(key, val);
  }
  let __wip = _dafny.Map.Empty;
  for (const [k, v] of (Object.entries(json.wip || {}) as [string, any][])) {
    const key = _dafny.Seq.UnicodeFromString(k);
    const val = new BigNumber(v);
    __wip = __wip.update(key, val);
  }
  let __cards = _dafny.Map.Empty;
  for (const [k, v] of (Object.entries(json.cards || {}) as [string, any][])) {
    const key = new BigNumber(k);
    const val = cardFromJson(v);
    __cards = __cards.update(key, val);
  }
  return KanbanDomain.Model.create_Model(
    _dafny.Seq.of(...(json.cols || []).map((x: any) => _dafny.Seq.UnicodeFromString(x))),
    __lanes,
    __wip,
    __cards,
    new BigNumber(json.nextId)
  );
};

// deno-lint-ignore no-explicit-any
const modelToJson = (value: any): Model => {
  const __lanesJson: Record<string, any> = {};
  if (value.dtor_lanes && value.dtor_lanes.Keys) {
    for (const k of value.dtor_lanes.Keys.Elements) {
      const v = value.dtor_lanes.get(k);
      __lanesJson[dafnyStringToJs(k)] = seqToArray(v).map((x: any) => toNumber(x));
    }
  }
  const __wipJson: Record<string, any> = {};
  if (value.dtor_wip && value.dtor_wip.Keys) {
    for (const k of value.dtor_wip.Keys.Elements) {
      const v = value.dtor_wip.get(k);
      __wipJson[dafnyStringToJs(k)] = toNumber(v);
    }
  }
  const __cardsJson: Record<string, any> = {};
  if (value.dtor_cards && value.dtor_cards.Keys) {
    for (const k of value.dtor_cards.Keys.Elements) {
      const v = value.dtor_cards.get(k);
      __cardsJson[toNumber(k)] = cardToJson(v);
    }
  }
  return {
    cols: seqToArray(value.dtor_cols).map((x: any) => dafnyStringToJs(x)),
    lanes: __lanesJson,
    wip: __wipJson,
    cards: __cardsJson,
    nextId: toNumber(value.dtor_nextId)
  };
};

// deno-lint-ignore no-explicit-any
const errFromJson = (json: any): DafnyErr => {
  switch (json) {
    case 'MissingColumn':
      return KanbanDomain.Err.create_MissingColumn();
    case 'MissingCard':
      return KanbanDomain.Err.create_MissingCard();
    case 'WipExceeded':
      return KanbanDomain.Err.create_WipExceeded();
    case 'BadAnchor':
      return KanbanDomain.Err.create_BadAnchor();
    case 'Rejected':
      return KanbanDomain.Err.create_Rejected();
    default:
      throw new Error(`Unknown Err: ${json}`);
  }
};

// deno-lint-ignore no-explicit-any
const errToJson = (value: any): Err => {
  if (value.is_MissingColumn) {
    return 'MissingColumn';
  } else if (value.is_MissingCard) {
    return 'MissingCard';
  } else if (value.is_WipExceeded) {
    return 'WipExceeded';
  } else if (value.is_BadAnchor) {
    return 'BadAnchor';
  } else if (value.is_Rejected) {
    return 'Rejected';
  }
  throw new Error('Unknown Err variant');
};

// deno-lint-ignore no-explicit-any
const optionFromJson = <T>(json: any, T_fromJson: (x: any) => T): DafnyOption<T> => {
  switch (json.type) {
    case 'None': {
      return KanbanDomain.Option.create_None();
    }
    case 'Some': {
      return KanbanDomain.Option.create_Some(
        T_fromJson(json.value)
      );
    }
    default:
      throw new Error(`Unknown Option type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const optionToJson = <T>(value: any, T_toJson: (x: any) => any): Option<T> => {
  if (value.is_None) {
    return { type: 'None' };
  } else if (value.is_Some) {
    return {
      type: 'Some',
      value: T_toJson(value.dtor_value)
    };
  }
  throw new Error('Unknown Option variant');
};

// deno-lint-ignore no-explicit-any
const placeFromJson = (json: any): DafnyPlace => {
  switch (json.type) {
    case 'AtEnd': {
      return KanbanDomain.Place.create_AtEnd();
    }
    case 'Before': {
      return KanbanDomain.Place.create_Before(
        new BigNumber(json.anchor)
      );
    }
    case 'After': {
      return KanbanDomain.Place.create_After(
        new BigNumber(json.anchor)
      );
    }
    default:
      throw new Error(`Unknown Place type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const placeToJson = (value: any): Place => {
  if (value.is_AtEnd) {
    return { type: 'AtEnd' };
  } else if (value.is_Before) {
    return {
      type: 'Before',
      anchor: toNumber(value.dtor_anchor)
    };
  } else if (value.is_After) {
    return {
      type: 'After',
      anchor: toNumber(value.dtor_anchor)
    };
  }
  throw new Error('Unknown Place variant');
};

// deno-lint-ignore no-explicit-any
const actionFromJson = (json: any): DafnyAction => {
  switch (json.type) {
    case 'NoOp': {
      return KanbanDomain.Action.create_NoOp();
    }
    case 'AddColumn': {
      return KanbanDomain.Action.create_AddColumn(
        _dafny.Seq.UnicodeFromString(json.col),
        new BigNumber(json.limit)
      );
    }
    case 'SetWip': {
      return KanbanDomain.Action.create_SetWip(
        _dafny.Seq.UnicodeFromString(json.col),
        new BigNumber(json.limit)
      );
    }
    case 'AddCard': {
      return KanbanDomain.Action.create_AddCard(
        _dafny.Seq.UnicodeFromString(json.col),
        _dafny.Seq.UnicodeFromString(json.title)
      );
    }
    case 'MoveCard': {
      return KanbanDomain.Action.create_MoveCard(
        new BigNumber(json.id),
        _dafny.Seq.UnicodeFromString(json.toCol),
        placeFromJson(json.place)
      );
    }
    case 'EditTitle': {
      return KanbanDomain.Action.create_EditTitle(
        new BigNumber(json.id),
        _dafny.Seq.UnicodeFromString(json.title)
      );
    }
    default:
      throw new Error(`Unknown Action type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const actionToJson = (value: any): Action => {
  if (value.is_NoOp) {
    return { type: 'NoOp' };
  } else if (value.is_AddColumn) {
    return {
      type: 'AddColumn',
      col: dafnyStringToJs(value.dtor_col),
      limit: toNumber(value.dtor_limit)
    };
  } else if (value.is_SetWip) {
    return {
      type: 'SetWip',
      col: dafnyStringToJs(value.dtor_col),
      limit: toNumber(value.dtor_limit)
    };
  } else if (value.is_AddCard) {
    return {
      type: 'AddCard',
      col: dafnyStringToJs(value.dtor_col),
      title: dafnyStringToJs(value.dtor_title)
    };
  } else if (value.is_MoveCard) {
    return {
      type: 'MoveCard',
      id: toNumber(value.dtor_id),
      toCol: dafnyStringToJs(value.dtor_toCol),
      place: placeToJson(value.dtor_place)
    };
  } else if (value.is_EditTitle) {
    return {
      type: 'EditTitle',
      id: toNumber(value.dtor_id),
      title: dafnyStringToJs(value.dtor_title)
    };
  }
  throw new Error('Unknown Action variant');
};

// deno-lint-ignore no-explicit-any
const resultFromJson = <T, E>(json: any, T_fromJson: (x: any) => T, E_fromJson: (x: any) => E): DafnyResult<T, E> => {
  switch (json.type) {
    case 'Ok': {
      return KanbanDomain.Result.create_Ok(
        T_fromJson(json.value)
      );
    }
    case 'Err': {
      return KanbanDomain.Result.create_Err(
        E_fromJson(json.error)
      );
    }
    default:
      throw new Error(`Unknown Result type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const resultToJson = <T, E>(value: any, T_toJson: (x: any) => any, E_toJson: (x: any) => any): Result<T, E> => {
  if (value.is_Ok) {
    return {
      type: 'Ok',
      value: T_toJson(value.dtor_value)
    };
  } else if (value.is_Err) {
    return {
      type: 'Err',
      error: E_toJson(value.dtor_error)
    };
  }
  throw new Error('Unknown Result variant');
};

// deno-lint-ignore no-explicit-any
const rejectreasonFromJson = (json: any): DafnyRejectReason => {
  return KanbanMultiCollaboration.RejectReason.create_DomainInvalid();
};

// deno-lint-ignore no-explicit-any
const rejectreasonToJson = (value: any): RejectReason => {
  return {};
};

// deno-lint-ignore no-explicit-any
const replyFromJson = (json: any): DafnyReply => {
  switch (json.type) {
    case 'Accepted': {
      return KanbanMultiCollaboration.Reply.create_Accepted(
        new BigNumber(json.newVersion),
        modelFromJson(json.newPresent),
        actionFromJson(json.applied),
        json.noChange
      );
    }
    case 'Rejected': {
      return KanbanMultiCollaboration.Reply.create_Rejected(
        rejectreasonFromJson(json.reason),
        actionFromJson(json.rebased)
      );
    }
    default:
      throw new Error(`Unknown Reply type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const replyToJson = (value: any): Reply => {
  if (value.is_Accepted) {
    return {
      type: 'Accepted',
      newVersion: toNumber(value.dtor_newVersion),
      newPresent: modelToJson(value.dtor_newPresent),
      applied: actionToJson(value.dtor_applied),
      noChange: value.dtor_noChange
    };
  } else if (value.is_Rejected) {
    return {
      type: 'Rejected',
      reason: rejectreasonToJson(value.dtor_reason),
      rebased: actionToJson(value.dtor_rebased)
    };
  }
  throw new Error('Unknown Reply variant');
};

// deno-lint-ignore no-explicit-any
const requestoutcomeFromJson = (json: any): DafnyRequestOutcome => {
  switch (json.type) {
    case 'AuditAccepted': {
      return KanbanMultiCollaboration.RequestOutcome.create_AuditAccepted(
        actionFromJson(json.applied),
        json.noChange
      );
    }
    case 'AuditRejected': {
      return KanbanMultiCollaboration.RequestOutcome.create_AuditRejected(
        rejectreasonFromJson(json.reason),
        actionFromJson(json.rebased)
      );
    }
    default:
      throw new Error(`Unknown RequestOutcome type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const requestoutcomeToJson = (value: any): RequestOutcome => {
  if (value.is_AuditAccepted) {
    return {
      type: 'AuditAccepted',
      applied: actionToJson(value.dtor_applied),
      noChange: value.dtor_noChange
    };
  } else if (value.is_AuditRejected) {
    return {
      type: 'AuditRejected',
      reason: rejectreasonToJson(value.dtor_reason),
      rebased: actionToJson(value.dtor_rebased)
    };
  }
  throw new Error('Unknown RequestOutcome variant');
};

// deno-lint-ignore no-explicit-any
const requestrecordFromJson = (json: any): DafnyRequestRecord => {
  return KanbanMultiCollaboration.RequestRecord.create_Req(
    new BigNumber(json.baseVersion),
    actionFromJson(json.orig),
    actionFromJson(json.rebased),
    actionFromJson(json.chosen),
    requestoutcomeFromJson(json.outcome)
  );
};

// deno-lint-ignore no-explicit-any
const requestrecordToJson = (value: any): RequestRecord => {
  return {
    baseVersion: toNumber(value.dtor_baseVersion),
    orig: actionToJson(value.dtor_orig),
    rebased: actionToJson(value.dtor_rebased),
    chosen: actionToJson(value.dtor_chosen),
    outcome: requestoutcomeToJson(value.dtor_outcome)
  };
};

// deno-lint-ignore no-explicit-any
const serverstateFromJson = (json: any): DafnyServerState => {
  return KanbanMultiCollaboration.ServerState.create_ServerState(
    modelFromJson(json.present),
    _dafny.Seq.of(...(json.appliedLog || []).map((x: any) => actionFromJson(x))),
    _dafny.Seq.of(...(json.auditLog || []).map((x: any) => requestrecordFromJson(x)))
  );
};

// deno-lint-ignore no-explicit-any
const serverstateToJson = (value: any): ServerState => {
  return {
    present: modelToJson(value.dtor_present),
    appliedLog: seqToArray(value.dtor_appliedLog).map((x: any) => actionToJson(x)),
    auditLog: seqToArray(value.dtor_auditLog).map((x: any) => requestrecordToJson(x))
  };
};

// deno-lint-ignore no-explicit-any
const clientstateFromJson = (json: any): DafnyClientState => {
  return KanbanMultiCollaboration.ClientState.create_ClientState(
    new BigNumber(json.baseVersion),
    modelFromJson(json.present),
    _dafny.Seq.of(...(json.pending || []).map((x: any) => actionFromJson(x)))
  );
};

// deno-lint-ignore no-explicit-any
const clientstateToJson = (value: any): ClientState => {
  return {
    baseVersion: toNumber(value.dtor_baseVersion),
    present: modelToJson(value.dtor_present),
    pending: seqToArray(value.dtor_pending).map((x: any) => actionToJson(x))
  };
};

// deno-lint-ignore no-explicit-any
const networkstatusFromJson = (json: any): DafnyNetworkStatus => {
  switch (json) {
    case 'Online':
      return KanbanEffectStateMachine.NetworkStatus.create_Online();
    case 'Offline':
      return KanbanEffectStateMachine.NetworkStatus.create_Offline();
    default:
      throw new Error(`Unknown NetworkStatus: ${json}`);
  }
};

// deno-lint-ignore no-explicit-any
const networkstatusToJson = (value: any): NetworkStatus => {
  if (value.is_Online) {
    return 'Online';
  } else if (value.is_Offline) {
    return 'Offline';
  }
  throw new Error('Unknown NetworkStatus variant');
};

// deno-lint-ignore no-explicit-any
const effectmodeFromJson = (json: any): DafnyEffectMode => {
  switch (json.type) {
    case 'Idle': {
      return KanbanEffectStateMachine.EffectMode.create_Idle();
    }
    case 'Dispatching': {
      return KanbanEffectStateMachine.EffectMode.create_Dispatching(
        new BigNumber(json.retries)
      );
    }
    default:
      throw new Error(`Unknown EffectMode type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const effectmodeToJson = (value: any): EffectMode => {
  if (value.is_Idle) {
    return { type: 'Idle' };
  } else if (value.is_Dispatching) {
    return {
      type: 'Dispatching',
      retries: toNumber(value.dtor_retries)
    };
  }
  throw new Error('Unknown EffectMode variant');
};

// deno-lint-ignore no-explicit-any
const effectstateFromJson = (json: any): DafnyEffectState => {
  return KanbanEffectStateMachine.EffectState.create_EffectState(
    networkstatusFromJson(json.network),
    effectmodeFromJson(json.mode),
    clientstateFromJson(json.client),
    new BigNumber(json.serverVersion)
  );
};

// deno-lint-ignore no-explicit-any
const effectstateToJson = (value: any): EffectState => {
  return {
    network: networkstatusToJson(value.dtor_network),
    mode: effectmodeToJson(value.dtor_mode),
    client: clientstateToJson(value.dtor_client),
    serverVersion: toNumber(value.dtor_serverVersion)
  };
};

// deno-lint-ignore no-explicit-any
const eventFromJson = (json: any): DafnyEvent => {
  switch (json.type) {
    case 'UserAction': {
      return KanbanEffectStateMachine.Event.create_UserAction(
        actionFromJson(json.action)
      );
    }
    case 'DispatchAccepted': {
      return KanbanEffectStateMachine.Event.create_DispatchAccepted(
        new BigNumber(json.newVersion),
        modelFromJson(json.newModel)
      );
    }
    case 'DispatchConflict': {
      return KanbanEffectStateMachine.Event.create_DispatchConflict(
        new BigNumber(json.freshVersion),
        modelFromJson(json.freshModel)
      );
    }
    case 'DispatchRejected': {
      return KanbanEffectStateMachine.Event.create_DispatchRejected(
        new BigNumber(json.freshVersion),
        modelFromJson(json.freshModel)
      );
    }
    case 'NetworkError': {
      return KanbanEffectStateMachine.Event.create_NetworkError();
    }
    case 'NetworkRestored': {
      return KanbanEffectStateMachine.Event.create_NetworkRestored();
    }
    case 'ManualGoOffline': {
      return KanbanEffectStateMachine.Event.create_ManualGoOffline();
    }
    case 'ManualGoOnline': {
      return KanbanEffectStateMachine.Event.create_ManualGoOnline();
    }
    case 'Tick': {
      return KanbanEffectStateMachine.Event.create_Tick();
    }
    default:
      throw new Error(`Unknown Event type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const eventToJson = (value: any): Event => {
  if (value.is_UserAction) {
    return {
      type: 'UserAction',
      action: actionToJson(value.dtor_action)
    };
  } else if (value.is_DispatchAccepted) {
    return {
      type: 'DispatchAccepted',
      newVersion: toNumber(value.dtor_newVersion),
      newModel: modelToJson(value.dtor_newModel)
    };
  } else if (value.is_DispatchConflict) {
    return {
      type: 'DispatchConflict',
      freshVersion: toNumber(value.dtor_freshVersion),
      freshModel: modelToJson(value.dtor_freshModel)
    };
  } else if (value.is_DispatchRejected) {
    return {
      type: 'DispatchRejected',
      freshVersion: toNumber(value.dtor_freshVersion),
      freshModel: modelToJson(value.dtor_freshModel)
    };
  } else if (value.is_NetworkError) {
    return { type: 'NetworkError' };
  } else if (value.is_NetworkRestored) {
    return { type: 'NetworkRestored' };
  } else if (value.is_ManualGoOffline) {
    return { type: 'ManualGoOffline' };
  } else if (value.is_ManualGoOnline) {
    return { type: 'ManualGoOnline' };
  } else if (value.is_Tick) {
    return { type: 'Tick' };
  }
  throw new Error('Unknown Event variant');
};

// deno-lint-ignore no-explicit-any
const commandFromJson = (json: any): DafnyCommand => {
  switch (json.type) {
    case 'NoOp': {
      return KanbanEffectStateMachine.Command.create_NoOp();
    }
    case 'SendDispatch': {
      return KanbanEffectStateMachine.Command.create_SendDispatch(
        new BigNumber(json.baseVersion),
        actionFromJson(json.action)
      );
    }
    case 'FetchFreshState': {
      return KanbanEffectStateMachine.Command.create_FetchFreshState();
    }
    default:
      throw new Error(`Unknown Command type: ${json.type}`);
  }
};

// deno-lint-ignore no-explicit-any
const commandToJson = (value: any): Command => {
  if (value.is_NoOp) {
    return { type: 'NoOp' };
  } else if (value.is_SendDispatch) {
    return {
      type: 'SendDispatch',
      baseVersion: toNumber(value.dtor_baseVersion),
      action: actionToJson(value.dtor_action)
    };
  } else if (value.is_FetchFreshState) {
    return { type: 'FetchFreshState' };
  }
  throw new Error('Unknown Command variant');
};

// ============================================================================
// Exports
// ============================================================================

export { _dafny, KanbanDomain, KanbanMultiCollaboration, KanbanEffectStateMachine, KanbanEffectAppCore, BigNumber };

export { cardFromJson, cardToJson, modelFromJson, modelToJson, errFromJson, errToJson, optionFromJson, optionToJson, placeFromJson, placeToJson, actionFromJson, actionToJson, resultFromJson, resultToJson, rejectreasonFromJson, rejectreasonToJson, replyFromJson, replyToJson, requestoutcomeFromJson, requestoutcomeToJson, requestrecordFromJson, requestrecordToJson, serverstateFromJson, serverstateToJson, clientstateFromJson, clientstateToJson, networkstatusFromJson, networkstatusToJson, effectmodeFromJson, effectmodeToJson, effectstateFromJson, effectstateToJson, eventFromJson, eventToJson, commandFromJson, commandToJson };

export { dafnyStringToJs, seqToArray, toNumber };

// ============================================================================
// ServerState Conversions (for verified Dispatch)
// ============================================================================

// deno-lint-ignore no-explicit-any
const serverStateFromJson = (json: any): any => {
  const present = modelFromJson(json.present);

  // Convert appliedLog: Action[] -> Dafny seq<Action>
  const appliedActions = (json.appliedLog || []).map(actionFromJson);
  const appliedLog = _dafny.Seq.of(...appliedActions);

  // Convert auditLog: AuditRecord[] -> Dafny seq<RequestRecord>
  const auditRecords = (json.auditLog || []).map((rec: any) => {
    const outcome = rec.outcome.type === 'accepted'
      ? KanbanMultiCollaboration.RequestOutcome.create_AuditAccepted(
          actionFromJson(rec.outcome.applied),
          rec.outcome.noChange || false
        )
      : KanbanMultiCollaboration.RequestOutcome.create_AuditRejected(
          KanbanMultiCollaboration.RejectReason.create_DomainInvalid(),
          actionFromJson(rec.outcome.applied || rec.rebased)
        );

    return KanbanMultiCollaboration.RequestRecord.create_Req(
      new BigNumber(rec.baseVersion),
      actionFromJson(rec.orig),
      actionFromJson(rec.rebased),
      actionFromJson(rec.chosen),
      outcome
    );
  });
  const auditLog = _dafny.Seq.of(...auditRecords);

  return KanbanMultiCollaboration.ServerState.create_ServerState(
    present,
    appliedLog,
    auditLog
  );
};

// deno-lint-ignore no-explicit-any
const serverStateToJson = (s: any): any => {
  const present = modelToJson(s.dtor_present);

  // Convert appliedLog: Dafny seq<Action> -> Action[]
  const appliedLog = seqToArray(s.dtor_appliedLog).map(actionToJson);

  // Convert auditLog: Dafny seq<RequestRecord> -> AuditRecord[]
  const auditLog = seqToArray(s.dtor_auditLog).map((rec: any) => {
    const outcome = rec.dtor_outcome;
    return {
      baseVersion: toNumber(rec.dtor_baseVersion),
      orig: actionToJson(rec.dtor_orig),
      rebased: actionToJson(rec.dtor_rebased),
      chosen: actionToJson(rec.dtor_chosen),
      outcome: outcome.is_AuditAccepted
        ? {
            type: 'accepted' as const,
            applied: actionToJson(outcome.dtor_applied),
            noChange: outcome.dtor_noChange
          }
        : {
            type: 'rejected' as const,
            reason: 'DomainInvalid',
            applied: actionToJson(outcome.dtor_rebased)
          }
    };
  });

  return { present, appliedLog, auditLog };
};

// ============================================================================
// dispatch - Verified Dispatch Function
// ============================================================================

export interface DispatchResult {
  status: 'accepted' | 'rejected';
  state?: any;
  appliedAction?: any;
  newVersion?: number;
  noChange?: boolean;
  appliedLog?: any[];
  auditLog?: any[];
  reason?: string;
}

export function dispatch(
  stateJson: any,
  appliedLog: any[],
  baseVersion: number,
  actionJson: any,
  auditLog?: any[]
): DispatchResult {
  // Build ServerState from JSON
  const serverState = serverStateFromJson({
    present: stateJson,
    appliedLog: appliedLog,
    auditLog: auditLog || []
  });

  // Call VERIFIED Dispatch
  const action = actionFromJson(actionJson);
  const result = KanbanMultiCollaboration.__default.Dispatch(
    serverState,
    new BigNumber(baseVersion),
    action
  );

  // Result is a tuple: [newServerState, reply]
  const newServerState = result[0];
  const reply = result[1];

  // Extract new state
  const newStateJson = serverStateToJson(newServerState);

  // Check reply type
  if (reply.is_Accepted) {
    return {
      status: 'accepted',
      state: newStateJson.present,
      newVersion: toNumber(reply.dtor_newVersion),
      appliedAction: actionToJson(reply.dtor_applied),
      noChange: reply.dtor_noChange,
      appliedLog: newStateJson.appliedLog,
      auditLog: newStateJson.auditLog
    };
  } else {
    // Rejected
    return {
      status: 'rejected',
      reason: 'DomainInvalid',
      appliedAction: actionToJson(reply.dtor_rebased)
    };
  }
}

